---
title: v4-主从高可用版
created: 2026-07-20
tags:
  - Redis
  - 毕业项目
  - v4
  - 主从复制
  - 哨兵
  - 方法论驱动
description: 搭建 1 主 2 从 + 3 哨兵，实现自动故障转移。
lark_doc_url: https://my.feishu.cn/docx/KZ8ddRVssoKossxRXMRcY0Ndnfb
---

# v4-主从高可用版

> 📍 **前置**：[[v3-持久化与AOF版]] → 本版本 → [[v5-Cluster分片版]]

## 🎯 本版本目标

搭建 1 主 2 从 + 3 哨兵，实现：**故障自动转移 + 客户端无缝切换**。

---

## 💻 核心实现

### 哨兵配置

```bash
# sentinel.conf
port 26379
sentinel monitor mymaster 127.0.0.1 6379 2
sentinel down-after-milliseconds mymaster 5000
sentinel failover-timeout mymaster 60000
sentinel parallel-syncs mymaster 1
```

### 启动集群

```bash
# 启动主节点和从节点
redis-server --port 6379 --daemonize yes
redis-server --port 6380 --daemonize yes --replicaof 127.0.0.1 6379
redis-server --port 6381 --daemonize yes --replicaof 127.0.0.1 6379

# 启动 3 个哨兵
redis-sentinel sentinel1.conf  # port 26379
redis-sentinel sentinel2.conf  # port 26380
redis-sentinel sentinel3.conf  # port 26381

# 查看状态
redis-cli -p 6379 INFO replication
redis-cli -p 26379 SENTINEL masters
```

### 应用层连接哨兵

```python
from redis.sentinel import Sentinel

sentinel = Sentinel([
    ('127.0.0.1', 26379),
    ('127.0.0.1', 26380),
    ('127.0.0.1', 26381),
], socket_timeout=0.5)

# 写走 Master
master = sentinel.master_for('mymaster')
master.set("key", "value")

# 读走 Replica
replica = sentinel.replica_for('mymaster')
print(replica.get("key"))
```

### 故障转移验证

```bash
# 模拟 Master 宕机
redis-cli -p 6379 SHUTDOWN

# 查看哨兵日志 → 新 Master 选举
redis-cli -p 26379 SENTINEL get-master-addr-by-name mymaster
# 自动切换到 6380 或 6381

# 应用层无需修改代码，Sentinel 自动切换
```

---

## ✅ 验收标准

- [ ] 1 主 2 从复制正常
- [ ] Master 宕机后 30 秒内自动切换
- [ ] 客户端自动连接到新 Master
- [ ] 旧 Master 恢复后自动成为 Replica

---

## 🔮 下一版本预告

当前单机存储容量有限。下一版本升级为 **Cluster 分片集群**。

---

## ✅ 自检清单

### 🟢 基础
- [ ] 主从复制搭建成功
- [ ] 故障自动转移验证

### 🟡 进阶
- [ ] 哨兵配置正确
- [ ] 客户端 Sentinel API 连接

### 🔴 挑战
- [ ] 能设计跨机房高可用方案
- [ ] 能分析脑裂场景并防护

---

*最后更新：2026-07-20*
