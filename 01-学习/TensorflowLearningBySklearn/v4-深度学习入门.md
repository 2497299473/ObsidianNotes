---
title: v4-深度学习入门
stage: 毕业项目
order: v4
difficulty: ⭐⭐⭐⭐
created: 2026-07-25
tags:
  - TensorFlow
  - 毕业项目
  - CNN
  - 数据增强
  - 迁移学习
  - 深度学习
description: 毕业项目 v4：用 TF 构建 CNN 图像分类模型 + 迁移学习微调——验证你能用 TF 做深度学习，Sklearn 无法完成的任务。
lark_doc_url: https://my.feishu.cn/docx/QsZBd8COAoDXooxm3Gkcdvlqnte
---

## 项目目标

用 CNN 做图像分类，用迁移学习提升精度。这是 Sklearn 无法完成的任务。

**只改一个维度**：从 Sklearn 无法做 CNN → TF 的 `Conv2D + 数据增强 + 迁移学习`。

## 前置知识

- [[08-深度学习入门-从MLP到Keras]]
- [[09-CNN与RNN-TF独有领域]]

---

## 任务 1：CNN 图像分类

### 数据

```python
import tensorflow as tf
import numpy as np

(x_train, y_train), (x_test, y_test) = tf.keras.datasets.cifar10.load_data()
x_train = x_train.astype(np.float32) / 255.0
x_test = x_test.astype(np.float32) / 255.0
y_train = y_train.flatten()
y_test = y_test.flatten()
```

### 要求

- [ ] 至少 3 个卷积块（Conv2D + BatchNorm + MaxPooling + Dropout）
- [ ] 使用数据增强层（RandomFlip + RandomRotation）
- [ ] 使用 GlobalAveragePooling2D 替代 Flatten
- [ ] 使用 EarlyStopping + ReduceLROnPlateau 回调
- [ ] 测试集准确率 > 70%

### 参考代码

```python
data_aug = tf.keras.Sequential([
    tf.keras.layers.RandomFlip('horizontal'),
    tf.keras.layers.RandomRotation(0.1),
])

model = tf.keras.Sequential([
    tf.keras.Input(shape=(32, 32, 3)),
    data_aug,
    tf.keras.layers.Conv2D(32, 3, padding='same', activation='relu'),
    tf.keras.layers.BatchNormalization(),
    tf.keras.layers.Conv2D(32, 3, padding='same', activation='relu'),
    tf.keras.layers.MaxPooling2D(),
    tf.keras.layers.Dropout(0.25),
    tf.keras.layers.Conv2D(64, 3, padding='same', activation='relu'),
    tf.keras.layers.BatchNormalization(),
    tf.keras.layers.Conv2D(64, 3, padding='same', activation='relu'),
    tf.keras.layers.MaxPooling2D(),
    tf.keras.layers.Dropout(0.25),
    tf.keras.layers.GlobalAveragePooling2D(),
    tf.keras.layers.Dense(128, activation='relu'),
    tf.keras.layers.Dropout(0.5),
    tf.keras.layers.Dense(10, activation='softmax')
])
model.compile(optimizer='adam', loss='sparse_categorical_crossentropy', metrics=['accuracy'])
model.fit(x_train, y_train, batch_size=64, epochs=50,
          validation_data=(x_test, y_test), verbose=0,
          callbacks=[
              tf.keras.callbacks.EarlyStopping(monitor='val_loss', patience=10, restore_best_weights=True),
              tf.keras.callbacks.ReduceLROnPlateau(monitor='val_loss', factor=0.5, patience=3)
          ])
_, acc = model.evaluate(x_test, y_test, verbose=0)
print(f"CNN 准确率: {acc:.4f}")
```

### 验收标准

- [ ] 测试集准确率 > 70%
- [ ] 使用数据增强层
- [ ] BatchNorm 在激活前

---

## 任务 2：迁移学习微调

### 要求

- [ ] 加载 ResNet50（`include_top=False`）
- [ ] 冻结基础层，训练新分类头（特征提取阶段）
- [ ] 解冻最后 20 层，微调（极小学习率 1e-5）
- [ ] 使用 GlobalAveragePooling2D + Dense 分类头

### 参考代码

```python
base_model = tf.keras.applications.ResNet50(
    weights='imagenet', include_top=False, input_shape=(224, 224, 3))
base_model.trainable = False

model = tf.keras.Sequential([
    base_model,
    tf.keras.layers.GlobalAveragePooling2D(),
    tf.keras.layers.Dense(256, activation='relu'),
    tf.keras.layers.Dropout(0.5),
    tf.keras.layers.Dense(10, activation='softmax')
])

# 特征提取阶段（较大学习率）
model.compile(optimizer=tf.keras.optimizers.Adam(1e-3),
              loss='sparse_categorical_crossentropy', metrics=['accuracy'])
model.fit(train_ds, epochs=10, validation_data=val_ds)

# 微调阶段（解冻 + 极小学习率）
base_model.trainable = True
for layer in base_model.layers[:-20]:
    layer.trainable = False
model.compile(optimizer=tf.keras.optimizers.Adam(1e-5),
              loss='sparse_categorical_crossentropy', metrics=['accuracy'])
model.fit(train_ds, epochs=5, validation_data=val_ds)
```

### 验收标准

- [ ] 特征提取阶段训练正常
- [ ] 微调学习率 ≤ 1e-5
- [ ] 能解释为什么微调学习率要极小（防止破坏预训练权重）

> 完成后进入 [[v5-端到端ML项目]]。

---
*创建时间：2026-07-25*