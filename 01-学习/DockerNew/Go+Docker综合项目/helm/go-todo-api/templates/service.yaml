apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-todo-api.fullname" . }}
  labels:
    {{- include "go-todo-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-todo-api.selectorLabels" . | nindent 4 }}
