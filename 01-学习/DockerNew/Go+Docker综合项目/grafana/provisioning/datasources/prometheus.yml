# Grafana 数据源自动配置
# 对应 DockerNew 第 10 章：监控可视化
# 容器启动时 Grafana 自动加载此配置，无需手动添加数据源

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: "15s"
