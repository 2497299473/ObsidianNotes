---
title: 03-构建工具与优化
created: 2026-07-19
stage: 3
order: 3
difficulty: ⭐⭐⭐⭐
estimated_hours: 5
tags:
  - React
  - 构建工具
lark_doc_url: https://my.feishu.cn/docx/RHPxd6p97oi4O5xUfonc1cignSg
---

## ⬅️ 前置知识
- [[02-测试React应用]] — 上一课内容

## ➡️ 后续笔记
- [[04-Next.js与SSR]] — 下一课

## 🔗 关联笔记
- [[../00-React学习路径总索引]] — 返回总索引
- [[../阶段三-工程化与全栈集成/99-阶段3复习检查点]] — 阶段复习

---

## 🎯 学习目标

学完本课后，你能够：
1. 理解 构建工具与优化 的核心概念与设计原理
2. 在本地环境编写并运行相关代码
3. 完成下方 🟢/🟡/🔴 三级自检

---

## 1. 核心概念

1. React.memo 组件记忆化
2. useMemo/useCallback 避免重渲染
3. React.lazy + Suspense 懒加载
4. 虚拟列表（react-window）
5. React Profiler 性能分析

---

## 2. 代码示例

```jsx
import React, { lazy, Suspense, useMemo, useCallback } from 'react';

// 代码分割 - 懒加载
const HeavyComponent = lazy(() => import('./HeavyComponent'));

function OptimizedList({ items }) {
  // useMemo 缓存计算结果
  const sorted = useMemo(() => {
    return [...items].sort((a, b) => a.localeCompare(b));
  }, [items]);

  const handleClick = useCallback((item) => {
    console.log('clicked:', item);
  }, []);

  return (
    <ul>
      {sorted.map(item => (
        <li key={item} onClick={() => handleClick(item)}>{item}</li>
      ))}
    </ul>
  );
}

export default function App() {
  return (
    <Suspense fallback={<div>加载中...</div>}>
      <OptimizedList items={['React', 'Vite', 'Webpack', 'ESBuild']} />
    </Suspense>
  );
}
`````

> 💡 请在本地环境中运行并修改以上示例，参考 React 官方文档获取最新 API 用法。

---

## 3. 常见陷阱

1. 只看不练，导致概念模糊——务必动手敲代码
2. 跳过前置基础，直接挑战高难度——按顺序学习
3. 不记录错误与解决方案——建立自己的错误笔记本

---

## ✏️ 综合练习

### 练习 1：概念复述
用自己的话解释「构建工具与优化」是什么，以及它解决了什么问题。

### 练习 2：代码实现
优化一个渲染缓慢的长列表，保存到 `code/` 目录。

### 练习 3：扩展思考
如果去掉本课知识点，项目代码会出现什么问题？

---

## 🎯 本课自检

| 层次 | 检查项 | 是否通过 |
|------|--------|----------|
| 🟢 基础 | 能复述 性能优化手段 的定义与用法 | [ ] |
| 🟡 进阶 | 能独立写出可运行的代码示例 | [ ] |
| 🔴 挑战 | 能在 React 项目中正确运用本课知识 | [ ] |

---

## 📊 通过标准

- 🟢 全部通过 → 可进入下一课
- 🟡 有未通过项 → 重做对应练习后重试
- 🔴 未通过 → 回看本课重点并补做笔记

---

*最后更新：2026-07-19*
