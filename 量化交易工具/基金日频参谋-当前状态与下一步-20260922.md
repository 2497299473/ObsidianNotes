# 基金日频参谋 · 当前状态与下一步（2026-09-22）

> **文档定位**：唯一权威状态快照（SSOT），接替 [[基金日频参谋-当前状态与下一步-20260902]]。
> 旧篇（0902）转为**版本史存档**：只增不改，其 `[ ]` 是当时快照，不再作进度依据。
> **更新纪律**：每次大版本、门禁变化、契约/口径变更、数据源状态变化时新开日期篇或在本篇追加收口；找进度只回本篇，找细节才回旧笔记。
> **本篇一切实测数字均于 2026-09-22 在 `D:\PythonProject\QuantV1\`（git tip `e44af41`）现场核得**，非转录旧篇。

---

## 一、当前版本与坐标

| 轴 | 值 |
|:--|:--|
| Project Generation | **V4 = 证据链闭环代**（本行为代际名唯一权威落点；V4.3.1 收口后现进入 **V4.4**） |
| Engine Version | 4.0.0-decision |
| Forecast Model Version | 3（B1 双列 14 维协议） |
| 代码根 | `D:\PythonProject\QuantV1\`（Windows 原生，09-08 自 WSL 迁入） |
| git tip | `e44af41`（V4.4 步 1 已提交并推送 origin/main） |
| 远程 | `github.com/2497299473/QuantProject.git`（分支 main） |

> ⚠️「到底 09-02 还是 09-22 才是当前状态？」——**09-22（本篇）**。0902 篇已被本篇接替，保留仅作历史。

---

## 二、生产门禁状态（截至 2026-09-22 14:55 审计）

来自 `output/audit_current.json`（`generated_at: 2026-09-22T14:55:34`）：

- audit_health = **FAIL**（17 PASS / 1 FAIL / 0 WARN）
- model_promotion = **BLOCKED**
- action_enable = **HOLD_ONLY**（动作层 `history_validated=false` 锁死）
- production_status = **BLOCKED**（`model_ready=false`）

> **审计健康 FAIL ≠ 生产可用**（四层独立，互不替代）。当前唯一 FAIL 见下节证据链。

### 🔴 当前证据链 P0：active 模型验证报告缺失（audit P1-2 FAIL）

- 现象：`forecast_v3.pkl`（active）绑定的验证报告 `output/backtest_forecast_v3_b1mask_20260831.log` **文件不存在**；`forecast_v2.pkl` 报告 `...v8_quantile_20260830.log` 同样缺失。
- 已核实：全仓扫描（排除 `.venv`）**两处报告均无法找回**——不只是 registry 指针悬空，**报告实体本身已不在盘上**。这是 GPT 评审「missing 验证报告」P0 的本地实证。
- 含义：无法用原始验证证据复核 active 模型的 RankIC / Brier 结论 → 晋升门（model_promotion）在证据补齐前**不应解除**。
- 处置方向：不是去"修审计让它 PASS"，而是**用新契约重算数据集重跑验证、产出可复现的新报告**（见第七节步 2/3）。

---

## 三、V4.4 本批（2026-09-22）核心变更：14:55 PIT Forecast 契约

### 拍板背景（Summer 定 label 分母口径）

现行 label：`fwd_h = navs[T+h]/navs[T] − 1`，分母是 **T 日最终 NAV**。但用户在 **14:55 决策时 T 日 NAV 尚未公布**（约当日 21~22 点才出）。以"当时看不到的价"作分母，语义不纯。

**拍板：label 分母改为「从 14:55 可观测状态起算」，全量重算。**

### 契约定义（唯一权威，落点 `core/pit1455_contract.py`）

```
nav_hat_1455(T) = navs[T−1] × (1 + est_chg)      # 14:55 时对 T 日 NAV 的最优估计
fwd_from_1455(h) = navs[T+h] / nav_hat_1455(T) − 1
```
- `navs[T−1]`：14:55 时点最后一个**已公布**净值（前一交易日），决策时必可见 → 成立。
- `est_chg`：**fraction 量纲**（0.0123 = 1.23%）。

### 🔴 本批核出的硬 P0：est_chg 量纲错位（100×）

| 侧 | 来源 | 量纲 | \|est_chg\| 范围 |
|:--|:--|:--|:--|
| 训练 / 冻结样本 | `backtest_spread` 用 `closes[j]/closes[j−1]−1` | **小数 fraction** | absmax 0.1798 |
| live / shadow 特征 | 腾讯行情 `parts[32]` 涨跌幅 | **百分数 pct** | 72 条 post 行 absmax 7.89、中位 1.29 |

**后果（实测）**：72 条 live post 行里 **66 条（91.7%）落在训练支撑域之外**；若 /100 归一，越界归零。`forecast_engine.predict` 无任何缩放（直接 `float(v)`）。同一 key 训练/推理差 100× —— 独立于原三 P0 的第四个硬伤。

> 本批**只登记换算桥 `est_chg_from_pct()` 与量纲常量 `EST_CHG_UNIT="fraction"`，未改 run.py / shadow 的现有产出**（避免一把梭）。接入是步 2：届时所有消费点统一过桥，**禁止各自 `*0.01` / `/100`**。

### 已落地（commit `e44af41`，零网络 / 只读 / 可回滚）

1. `core/pit1455_contract.py`（新增，纯函数）：契约式 + `est_chg_from_pct()` + `contract_provenance()`。
2. `pit1455_dataset.py`（新增，零网络只读）：冻结样本逐行套契约重算 → `forecast_outputs/samples_pit1455_20260922.jsonl` + meta 侧车；原字段全保留、新 label 以 `*_1455` 并存，**不原位覆盖**（可对照、可回滚）。
3. `tests/test_pit1455_contract.py`（新增，fast 层）：8 项，含**黄金校验**——est_chg 取 T 日真实涨跌时新 label 必须退化为原 label（锁死"分母取 T−1、索引 i+h 对齐"）。

### 全量实跑数字（2026-09-22）

- 冻结样本 `samples_frozen_20260910.jsonl` 3371 行，重算 **3371/3371 零跳过**。
- 新旧 label 位移：fwd1 mean|Δ|=0.00631 / max|Δ|=0.1455；fwd20 mean|Δ|=0.00644 / max|Δ|=0.1706。
  → **分母口径非可忽略项**（mean 0.63%、max 14.5%），必须重算而非微调。
- 抽行独立复算校验：002112 / 2025-05-28 手工重算 `fwd5_1455=0.107148` 与落盘值一致；`|label|>0.5` 异常行 0 条。
- **基线上涨率随分母口径移动（步 3 的基准必换）**：`P(fwd>0)` 旧→新 = fwd1 49.57%→49.57%、fwd3 51.38%→51.20%、fwd5 52.80%→51.97%。
  → 旧口径把 T 日尾盘涨幅记进分母，**人为抬高**了中短周期的"上涨"基准；步 3 若仍拿 52.8% 当多数类基线，会系统性高估模型命中。**验收矩阵一律用 `*_1455` 列重新起算**。

---

## 四、门禁状态总览（沿用 V4.3 口径，摘自 README Project State）

| 维度 | 状态 |
|:--|:--|
| T+5 方向 | **UNRESOLVED**（09-18 C2/CTRL WF 部署式 pooled RankIC −0.079 CI[−0.158,−0.016]；+0.080 已因特征非不变性降为历史档案） |
| Walk-Forward | **PARTIAL**（重训救不回最近两折 → regime shift 非模型老化） |
| T+3 三拆（研究轨） | 三尺度均不显著，「仅点估计」维持 |
| Policy | **BLOCKED**（ML 超额 −3.041% vs 单因子 +0.167%，θ=0.6 预注册不动） |
| Shadow Policy | **ENABLED (paper-only)**，每条内嵌 Evidence Contract |
| Market Context | v0.1 封版、观察期（指标定义冻结至满 60 有效交易日） |
| Forecast/Policy 整合 | **LOCKED**（Market Context 仅描述性，不进 Forecast/Policy/门禁） |

---

## 五、V4.3.1 收口批次（2026-09-21~22，本批前置）

- **⑤ 冻结链严格化 + registry provenance 门禁**：显式 `--kfp` 严格模式（坏 JSON/标签不符/scope 不符→UNKNOWN 拒回退）；冻结 fund 侧 scope（无关基金缓存变化不改冻结指纹，09-10 锚点逐字节不变）；`verify_approval` 要求 `snapshot_provenance` 五键齐全（FROZEN）才可对外展示。fast 597→606。
- **⑥ LOFO 证据可追溯 + 陈旧标记上链**：证据产出绑定三件套（status/valid_for）；08-30 存量件就地标 STALE（不伪造可追溯性）；消费端出陈旧标记但 **confidence 数值一字不变**（回归测例钉死）；Path-WF 措辞对齐事实（每折重估**池级** μ/σ，非基金级）。测试 606→613。

---

## 六、开放问题与决策账（已结案）

- ✅ **label 分母**：从 14:55 可观测状态起算，全量重算（本批已落契约 + 数据集）。
- ✅ **SSOT 指针**：新写 09-22 篇（= 本篇），接替 0902。
- ✅ **下一步优先级 A/B/C 之择**：确认「先修证据链」，但执行序按「零网络可回滚优先」= 先 1（契约+量纲桥）→ 2（重算构建器）→ 3（验收矩阵）→ 4（SSOT）。1、2、4 已完成；证据链的"重训+重跑验证报告"依赖量纲接入，排步 2 之后。

---

## 七、下一步落点（按优先级）

1. ✅ **步 1**：14:55 契约 + est_chg 量纲桥登记 + 重算构建器（已完成，`e44af41`）。
2. ✅ **步 2**：**量纲接入**（2026-09-22 晚 Summer 授权执行，commit `0ba2733`）——run.py build_forecast_meta / decision_engine fallback 统一过 `est_chg_from_pct()`；store 只追加不覆写 ⇒ 契约新增切换日 `EST_CHG_LIVE_FRACTION_SINCE="2026-09-23"`，shadow / score_expert_a 读取端按行日期判别（旧行过桥、新行透传）；shadow 镜像分走逆向桥还原 % 域并留 `est_chg_fraction` 原值。**裁量：不 bump MODEL_VERSION、不重训换权重**——训练样本本就是 fraction（错在 live 输入侧），且 prereg 授权把 v3 sha 钉死至 10-12，换权重即断今晚 shadow 降级授权；「重训 v4」留待拍板。验证重跑消费 0910 冻结件（G-A PASS / G-B DRIFTED），两次运行字节级一致，T+1/3/5 全 rejected（rank_ic +0.049/+0.048/−0.019 CI 跨零）→ 维持 model_ready=false；报告 `output/backtest_forecast_v44_step2_20260922.log` 仅留档未 bind_validation（护住 prereg D3，v3 现行授权不受影响）。全程零东财请求（冻结快照 + 本地指纹重算），铁律 7/8 以「无请求」满足。fast 471→477、slow 158 OK。
3. ⏭️ **步 3**：**四基金 × 多周期验收矩阵** —— 用 `samples_pit1455_20260922.jsonl` 重算各基金 T+1/3/5 的 Brier / RankIC / 校准，对照旧 label 口径，回答"模型有没有把强的排在弱的前面""63% 上涨概率是否大致真 63%"。零网络可先行。
4. ✅ **步 4**：SSOT 新篇 + 总览索引指针切换（本篇 + 索引 39/50 行改指 0922）。
5. 观察层样本积累（market_context / intraday delta）与 09-26 阶段性复核继续，均**≠ 晋升评估**；**09-26 附加议程**：① 验证 ❌ 与 09-08 `--fresh` 历史报告的口径差复核（冻结 vs 活拉不可比）；② 「是否重训 v4」拍板（需同步改 prereg 授权 + 新冻结件重训 + bind_validation 一次到位）。

---

## 八、相关笔记

- [[基金日频参谋-当前状态与下一步-20260902]] — 已被本篇接替，转历史存档
- [[量化交易工具-总览索引]] — 导航索引（SSOT 指针已切 0922）
- [[C2生产轨三拆与特征非不变性-20260918]] — est_chg 特征非时点不变性（量纲 P0 的前序线索）
- [[冻结输入三件套锁定-D3与施工事故-20260918]] — 冻结件三件套口径

---

*本篇由 OpenSquilla 于 2026-09-22 创建，接替 0902 篇成为 SSOT。*
