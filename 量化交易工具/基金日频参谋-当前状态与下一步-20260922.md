# 基金日频参谋 · 当前状态与下一步（2026-09-22）

> **文档定位**：唯一权威状态快照（SSOT），接替 [[基金日频参谋-当前状态与下一步-20260902]]。
> 旧篇（0902）转为**版本史存档**：只增不改，其 `[ ]` 是当时快照，不再作进度依据。
> **更新纪律**：每次大版本、门禁变化、契约/口径变更、数据源状态变化时新开日期篇或在本篇追加收口；找进度只回本篇，找细节才回旧笔记。
> **本篇实测数字来源**：创建时（2026-09-22，tip `e44af41`）一、二、三节现场核得；五-B 节（同日晚，tip `b1fcd22`）与五-C 节（2026-09-23，tip `89be48e`）为后续追加收口，均在各自批次现场核得，非转录旧篇。

---

## 一、当前版本与坐标

| 轴 | 值 |
|:--|:--|
| Project Generation | **V4 = 证据链闭环代**（本行为代际名唯一权威落点；V4.3.1 收口后现进入 **V4.4**） |
| Engine Version | 4.0.0-decision |
| Forecast Model Version | 3（B1 双列 14 维协议） |
| 代码根 | `D:\PythonProject\QuantV1\`（Windows 原生，09-08 自 WSL 迁入） |
| git tip | `b38249f`（2026-09-24 A 批收口：血统对账 + stock cache + SSOT 审计指针；前序 `fc4f130`（V4.4 步 1–3 + P0 批 + 重定批 + 停摆修复 + 债务批，详见五-F~五-H）→ **evidence 证据层 `e983354`**（首份验证报告在档；`51d2806` FAILED 清单补齐 / `194f0be` registry 路径归一化）→ **晋升契约 B 批 `35913a1`（B1/B2/B3，详见五-I）** → 运行留档 `1d3d994`，均已推送 origin/main） |
| 远程 | `github.com/2497299473/QuantProject.git`（分支 main） |

> ⚠️「到底 09-02 还是 09-22 才是当前状态？」——**09-22（本篇）**。0902 篇已被本篇接替，保留仅作历史。

---

## 二、生产门禁状态（截至 2026-09-22 14:55 审计）

来自 `output/audit_current.json`（`generated_at: 2026-09-22T14:55:34`）：

- audit_health = **FAIL**（17 PASS / 1 FAIL / 0 WARN）
- model_promotion = **BLOCKED**
- action_enable = **HOLD_ONLY**（动作层 `history_validated=false` 锁死）
- production_status = **BLOCKED**（`model_ready=false`）

> **审计健康 FAIL ≠ 生产可用**（四层独立，互不替代）。当前唯一 FAIL 见下节证据链。

### 🔴 当前证据链 P0：active 模型验证报告缺失（audit P1-2 FAIL）

- 现象：`forecast_v3.pkl`（active）绑定的验证报告 `output/backtest_forecast_v3_b1mask_20260831.log` **文件不存在**；`forecast_v2.pkl` 报告 `...v8_quantile_20260830.log` 同样缺失。
- 已核实：全仓扫描（排除 `.venv`）**两处报告均无法找回**——不只是 registry 指针悬空，**报告实体本身已不在盘上**。这是 GPT 评审「missing 验证报告」P0 的本地实证。
- 含义：无法用原始验证证据复核 active 模型的 RankIC / Brier 结论 → 晋升门（model_promotion）在证据补齐前**不应解除**。
- 处置方向：不是去"修审计让它 PASS"，而是**用新契约重算数据集重跑验证、产出可复现的新报告**（见第七节步 2/3）。

---

## 三、V4.4 本批（2026-09-22）核心变更：14:55 PIT Forecast 契约

### 拍板背景（Summer 定 label 分母口径）

现行 label：`fwd_h = navs[T+h]/navs[T] − 1`，分母是 **T 日最终 NAV**。但用户在 **14:55 决策时 T 日 NAV 尚未公布**（约当日 21~22 点才出）。以"当时看不到的价"作分母，语义不纯。

**拍板：label 分母改为「从 14:55 可观测状态起算」，全量重算。**

### 契约定义（唯一权威，落点 `core/pit1455_contract.py`）

> ⚠️ 2026-09-23 更新：Forecast Contract 重定（`forecast-v1`，见五-E）后，**裁决口径权威 = 分母 T 日最终 NAV**（= 生产口径，一字未变）；本节的 1455 label 降级为「仅决策输入 + 盈亏核算」，其式子权威仍指本节。裁决基线写死 est_chg 单因子。

```
nav_hat_1455(T) = navs[T−1] × (1 + est_chg)      # 14:55 时对 T 日 NAV 的最优估计
fwd_from_1455(h) = navs[T+h] / nav_hat_1455(T) − 1
```
- `navs[T−1]`：14:55 时点最后一个**已公布**净值（前一交易日），决策时必可见 → 成立。
- `est_chg`：**fraction 量纲**（0.0123 = 1.23%）。

### 🔴 本批核出的硬 P0：est_chg 量纲错位（100×）

| 侧 | 来源 | 量纲 | \|est_chg\| 范围 |
|:--|:--|:--|:--|
| 训练 / 冻结样本 | `backtest_spread` 用 `closes[j]/closes[j−1]−1` | **小数 fraction** | absmax 0.1798 |
| live / shadow 特征 | 腾讯行情 `parts[32]` 涨跌幅 | **百分数 pct** | 72 条 post 行 absmax 7.89、中位 1.29 |

**后果（实测）**：72 条 live post 行里 **66 条（91.7%）落在训练支撑域之外**；若 /100 归一，越界归零。`forecast_engine.predict` 无任何缩放（直接 `float(v)`）。同一 key 训练/推理差 100× —— 独立于原三 P0 的第四个硬伤。

> 本批**只登记换算桥 `est_chg_from_pct()` 与量纲常量 `EST_CHG_UNIT="fraction"`，未改 run.py / shadow 的现有产出**（避免一把梭）。接入是步 2：届时所有消费点统一过桥，**禁止各自 `*0.01` / `/100`**。

### 已落地（commit `e44af41`，零网络 / 只读 / 可回滚）

1. `core/pit1455_contract.py`（新增，纯函数）：契约式 + `est_chg_from_pct()` + `contract_provenance()`。
2. `pit1455_dataset.py`（新增，零网络只读）：冻结样本逐行套契约重算 → `forecast_outputs/samples_pit1455_20260922.jsonl` + meta 侧车；原字段全保留、新 label 以 `*_1455` 并存，**不原位覆盖**（可对照、可回滚）。
3. `tests/test_pit1455_contract.py`（新增，fast 层）：8 项，含**黄金校验**——est_chg 取 T 日真实涨跌时新 label 必须退化为原 label（锁死"分母取 T−1、索引 i+h 对齐"）。

### 全量实跑数字（2026-09-22）

- 冻结样本 `samples_frozen_20260910.jsonl` 3371 行，重算 **3371/3371 零跳过**。
- 新旧 label 位移：fwd1 mean|Δ|=0.00631 / max|Δ|=0.1455；fwd20 mean|Δ|=0.00644 / max|Δ|=0.1706。
  → **分母口径非可忽略项**（mean 0.63%、max 14.5%），必须重算而非微调。
- 抽行独立复算校验：002112 / 2025-05-28 手工重算 `fwd5_1455=0.107148` 与落盘值一致；`|label|>0.5` 异常行 0 条。
- **基线上涨率随分母口径移动（步 3 的基准必换）**：`P(fwd>0)` 旧→新 = fwd1 49.57%→49.57%、fwd3 51.38%→51.20%、fwd5 52.80%→51.97%。
  → 旧口径把 T 日尾盘涨幅记进分母，**人为抬高**了中短周期的"上涨"基准；步 3 若仍拿 52.8% 当多数类基线，会系统性高估模型命中。**验收矩阵一律用 `*_1455` 列重新起算**。

---

## 四、门禁状态总览（沿用 V4.3 口径，摘自 README Project State）

| 维度 | 状态 |
|:--|:--|
| T+5 方向 | **UNRESOLVED**（09-18 C2/CTRL WF 部署式 pooled RankIC −0.079 CI[−0.158,−0.016]；+0.080 已因特征非不变性降为历史档案） |
| Walk-Forward | **PARTIAL**（重训救不回最近两折 → regime shift 非模型老化） |
| T+3 三拆（研究轨） | 三尺度均不显著，「仅点估计」维持 |
| Policy | **BLOCKED**（ML 超额 −3.041% vs 单因子 +0.167%，θ=0.6 预注册不动） |
| Shadow Policy | **ENABLED (paper-only)**，每条内嵌 Evidence Contract |
| Market Context | v0.1 封版、观察期（指标定义冻结至满 60 有效交易日） |
| Forecast/Policy 整合 | **LOCKED**（Market Context 仅描述性，不进 Forecast/Policy/门禁） |

---

## 五、V4.3.1 收口批次（2026-09-21~22，本批前置）

- **⑤ 冻结链严格化 + registry provenance 门禁**：显式 `--kfp` 严格模式（坏 JSON/标签不符/scope 不符→UNKNOWN 拒回退）；冻结 fund 侧 scope（无关基金缓存变化不改冻结指纹，09-10 锚点逐字节不变）；`verify_approval` 要求 `snapshot_provenance` 五键齐全（FROZEN）才可对外展示。fast 597→606。
- **⑥ LOFO 证据可追溯 + 陈旧标记上链**：证据产出绑定三件套（status/valid_for）；08-30 存量件就地标 STALE（不伪造可追溯性）；消费端出陈旧标记但 **confidence 数值一字不变**（回归测例钉死）；Path-WF 措辞对齐事实（每折重估**池级** μ/σ，非基金级）。测试 606→613。

---

## 五-B、V4.4-步3 验收矩阵（2026-09-22 晚，零网络，commit `b1fcd22`）

**脚本** `backtest_pit1455_matrix.py`；**产物** `output/backtest_pit1455_matrix_20260922.md`（人读）+ `forecast_outputs/pit1455_matrix_20260922.json`（机读，.gitignore 拦截、本地留档）。

### 三轨设计（把「模型血统差」与「label 口径差」分开）

| 轨 | 含义 | 用途 |
|:--|:--|:--|
| **A** | 线上现行 `forecast_v3.pkl` 权重**只推理**（旧 label 所训） | 读「若现在上线，两种口径各报什么」 |
| **C** | 旧 label **重训** | **自校验轨**：复现步 2 即证明指标实现与生产验证器等价 |
| **B** | 1455 label **重训**（反事实，`--retrain`） | 回答「若改用 1455 训练会怎样」 |

> ⚠️ A ≠ 步 2 基准（A 是线上冻结权重，步 2 是 in-process 重训）。**先核 C 轨**，勿拿 A 轨直接对步 2。

切分真源 = **0910 冻结件**（train < 2025-05-14：2438 / OOS：923），PIT 件只按 `(fund,date)` 贴 `*_1455` 列 ⇒ 池成员与历史报告逐行一致（直接对 PIT 件重切会推宽 train，与线上不可比）。等价性守卫断言 `build_xy` 行过滤/取值/特征矩阵与本脚本逐位一致。

### ① 自校验通过（这是 1455 列可信的前提）

| 周期 | 矩阵 C 轨 old | 步 2 基准 | \|Δ\| |
|:--|--:|--:|--:|
| T+1 | +0.0485 | +0.049 | 0.0005 |
| T+3 | +0.0480 | +0.048 | 0.0000 |
| T+5 | −0.0191 | −0.019 | 0.0001 |

### ② 🔴 本步最关键发现：1455 口径的「机械耦合」——基线必须从 0 换成 −est_chg

契约分母含 `est_chg`，而 `est_chg` 同时是模型输入特征 ⇒ 机械耦合。精确恒等式（实测残差 5e-7，舍入级）：

```
1 + fwd_1455 = (1 + fwd_old) · (1 + a_T) / (1 + est_chg)
```

（`a_T` = T 日真实涨跌。实测 label 位移与 `a_T − est_chg` 的 Spearman = +0.9998 ⇒ 位移几乎全由「est_chg 估偏多少」驱动。）

| 口径 | 诚实基线 | 池级 OOS 基线 IC（T+1/3/5，n=923） |
|:--|:--|:--|
| old | `+est_chg` | −0.013 / +0.025 / −0.009（≈0，与 0 比成立） |
| **1455** | **`−est_chg`** | **+0.1278 / +0.0393 / +0.0567**（必须与它比） |

⇒ 零成本公式「预测分 = −est_chg」不训练就有正 IC。B 轨 1455 IC +0.1139（T+1）看着漂亮，**超额 = −0.0139，跑输该基线**；A/C 轨 T+1 同样不及。**1455 口径下「模型有排序能力」的结论不成立。**

- **契约式一字未改**（分母含 est_chg 正是「14:55 可观测」的定义所在），只把「1455 裁决基线必须是 −est_chg」写进 `core/pit1455_contract.py` 的消费纪律注释。
- **更干净的替代口径（留 Summer 拍板）**：预测目标应仍是 old 口径（T→T+h 真实收益）；1455 只用于「决策时可得的输入」与「相对 14:55 估计价的盈亏核算」，不该塞进 label 分母后再拿模型分与它比。

### ③ Q2 校准回答：现行模型是**低估**而非高估

C 轨 mean p_up 0.400/0.465/0.477 系统性**低于**实际上涨率（偏差 −0.059/−0.036/−0.036）。A/C 轨池级 ACE 0.039~0.092、ECE 0.041~0.082；B 轨（1455 训练）校准明显退化（ACE 0.121~0.155）⇒ 1455 训练把概率整体推高。0.60~0.80 高概率桶 n=2~54 属稀疏区，校准斜率不可信。

### ④ 基线换口径确证（OOS 段，补步 1 的全样本口径）

`P(fwd>0)` old→1455 = 0.5114→0.5070 / 0.5276→0.5298 / 0.5385→0.5298。方向与步 1 全样本一致，T+5 的 old 被人为抬高最明显（53.85%→52.98%）。

### ⑤ G-B DRIFTED 澄清（新旧对照有没有被缓存漂移污染）

按当前 `data/klines` 重算**旧口径** label 与 0910 存量比：mean|Δ| = max|Δ| = **0**（3371 行 × 3 周期全匹配）⇒ 净值层与冻结时点一致，**新旧对照是纯口径差**。（G-B 指纹仍 DRIFTED：变的是 K 线聚合口径，不是历史净值被改写。）

### ⑥ 逐基金（C 轨 T+1；主轨取 C 以隔离模型血统）

| 基金 | n | RankIC(old) | 95% CI |
|:--|--:|--:|:--|
| 002112 | 306 | +0.0601 | [−0.056, +0.172] |
| 002207 | 306 | +0.1034 | [−0.009, +0.212] |
| 022853 | 276 | −0.0042 | [−0.123, +0.112] |
| 025687 | 35 | −0.1325 | [−0.449, +0.190] ⚠️ LOW_N |

全池 CI 跨零，与步 2 一致。022853/025687 无训练段样本（新基金）⇒ 其格是「旧 label 训练的模型预测新基金」的纯迁移读数。

### 测例与验证

- `tests/test_pit1455_matrix.py`（fast 层 14 项，合成行不碰 `data/`）：基线方向符号/超额、`_feature_matrix` ≡ `build_xy` 协议等价、缺失 mask 列、低样本与常量输入守卫、分窗守卫、PIT 重复键 fail-closed、步 2 基准常量。
- 实跑 **fast 474→488、slow 158 = 646 OK(skipped=3)**，零回归。
  ⚠️ **基数对账坑（延续步 1 教训）**：步 2 README 记 fast 477，本步实测基线 **474**（差 3，疑步 2 后测试计数变动未同步）⇒ 历史数字只作参考，**以 `run_tests.py --layer fast/slow` 实跑为准**。
- 全程零东财请求（socket 守卫默认开），未改任何生产脚本 / 冻结件 / `model_ready` / 门禁。

---

## 五-C、V4.4-附：数据源 P0-2 修复（2026-09-23，Summer 授权，commit `89be48e`）

- **来源**：GPT 外部评审「P0-2 腾讯分页中途失败仍 ok=True」，现场核实成立；但这是写进 docstring 的**有意迁移行为**（旧实现即如此），修它 = 改语义，需人授权——2026-09-23 Summer 拍板修。
- **危害链**：分页中某页失败时拿已取页出 `ok=True` ⇒ 截断历史静默写入 `data/stock_klines/` 缓存（TTL 12h 内所有消费方复用），污染下游 lookthrough / market_context / 面板刷新。
- **修法**：`err` 存在即整源判败，错误文本带 `partial_page: got=N/5`（首页即败的旧格式逐字不变）；前缀仍走 `classify_exc`——网络类计入健康度连续失败（达 3 次降级链尾），非传输类归 `data:` 不冤枉降级；`run_chain` 见 ok=False 继续走链，由东财等下一源补全量。
- **测例**：`test_partial_page_failure_keeps_fetched_pages` 改名 `..._is_not_success`（断言反转：ok=False / payload=None / network: 前缀 / partial_page 文本）；新增 data 类前缀中途失败 1 项。实跑 **fast 489（步 3 基线 488，净 +1）、slow 158 OK**，零回归；全程 stub netutil，零东财请求，铁律 7/8 以「无请求」满足；未动任何门禁/冻结件/模型权重。
- **同日另一观察点**：今天正是 `EST_CHG_LIVE_FRACTION_SINCE="2026-09-23"` 切换日，今晚 22:30 shadow 是切换后首个夜跑周期（旧行过桥、新行透传），晨检/盘后应见首批 fraction 量纲的 live 行。

---

## 五-D、V4.4-P0 批：四项零网络修/验证（2026-09-23，D0，commits `ef6d7c1` / `5925a3e`）

来源：GPT 外部评审 P0 清单（2026-09-23 逐条对仓库核实后，Summer 授权「D0 开工」）。全批零网络、零东财请求，可回滚。

- **P0-1 两轨 decision_edge 审计指标**：`backtest_forecast.py` 新增 `decision_edge_metrics` 纯函数——OOS 同样本、同 fwd 标签（**NAV[T] 标签口径不变**）下 `RankIC(模型) − RankIC(est_chg 单因子)`，回答「相对 14:55 估值机械基线，模型额外贡献多少排序信息」；CI 复用 `cluster_bootstrap_ci`（按日块），**只审计、不进 ok 裁决**。新增 `tests/test_decision_edge.py` 4 项（fast 层，合成数据）。离线重算（0910 冻结件 G-A PASS / G-B DRIFTED，零网络）：T+1 **+0.0615** [−0.042, +0.169] / T+3 **+0.0229** [−0.076, +0.115] / T+5 **−0.0106** [−0.127, +0.103]，CI 全跨零 ⇒ 无稳定超额排序信息，与五-B「1455 口径 −est_chg 基线」结论同向，维持 model_ready=false。
- **P0-2 校准口径复查**：`calibration_curve` 全仓**单一实现**（`backtest_forecast.py:55`），lofo / pit1455 / t5_scorecard 全部 import 复用；ACE 的 `mid` 口径无同类分叉 ⇒ **不改，留档闭合**。另发现 `forecast_engine.py:159` `self.calib` 为死字段（从未赋值，仅注释）——只报告，未动。
- **P0-3 Obsidian 流水路径配置化**：`config.json` 新增 `notify.obsidian_signal_log` 为单一事实来源（相对路径按项目根解析；留空/删键 = 禁用，静默跳过**不算 degraded**；config 缺失/损坏回退旧默认路径）。`run.py` `append_obsidian_log` 默认参数由 `OBSIDIAN_LOG` 改 `None` 走 `_configured_obsidian_log()`（修掉「默认值绕过配置」夹缝）。新增 `TestObsidianLogConfigured` 6 项（tempdir 零网络，不碰真实笔记）。
- **P0-4 registry / 门禁复算**：`data/models/` 两 pkl 的 sha256 与 size 同 registry **全一致**（v2 `a0c30964…` / v3 `7b66ce12…`）；门禁链自洽 fail-closed（audit P1-1/3/4 PASS）。P1-2 FAIL 为**既有状态**（两份验证报告因 `output/*.log` gitignore 从未入库、实体已不在盘上，即第二节 🔴 P0），本批不动，处置方向不变：新契约重算 + 重跑验证产出新报告后 bind_validation。
- **测试环境记录（防再踩坑）**：`D:\Python`（3.14.6）缺 scipy/sklearn，**不是正式环境**——用它跑 discover 会出 11 个环境性假失败；正式环境 = 项目 `.venv`（3.12.13）。本批终态（`.venv`）：**fast 499**（基线 489 + 新增 10）、**全量 657 OK**，零回归。

---

## 五-E、V4.4-重定批：Forecast Contract（forecast-v1）+ lookthrough 报告侧 PIT 修复（2026-09-23，Summer 批准，commits `a73f1ed` / `040c26e`）

来源：GPT 外部评审「到底什么叫一次有效的预测？」+ 步 3 机械耦合发现；Summer 拍板「A 的合同文本 + contract test 骨架，带上 lookthrough `history[-1]` 一行修复」。全批零网络、零东财请求，可回滚。

- **Forecast Contract 权威文本**（`core/forecast_contract.py` 新模块，C1–C5 纪律）：
  - **C1 裁决 label 钉死分母 = T 日最终 NAV**（= 14:55 订单真执行价；生产口径本就如此，一字未变）；**est_chg 不进 label**，输入/输出耦合在定义上拆掉；
  - **C2 1455 口径降级为「仅决策输入 + 盈亏核算」**——不进 label / 训练目标 / 晋升门禁；式子权威仍 pit1455-v1（该文件仅追加角色声明，公式一字未改，防两处漂移）；
  - **C3 裁决基线写死 est_chg 单因子**（decision_edge = RankIC(模型) − RankIC(est_chg)，实现见 P0-1 批）；若未来再把 1455 拿来做裁决目标，必须先预注册 −est_chg 基线，事后补无效；
  - **C4 量纲 fraction**（唯一换算桥 `est_chg_from_pct()`）；
  - **C5 机械耦合恒等式存档为降级依据**（恒等式残差 5e-7 / 步 3 九格仅 1 格跑赢基线 / decision_edge CI 全跨零，三重证据同向）。
- **contract test 骨架**（`tests/test_forecast_contract.py` 9 项，fast 层，合成行零网络）：C1 黄金校验（与 `backtest_spread` 公式逐位一致）、est_chg 不可入裁决 label 参数断言、基线方向（+est_chg / −est_chg / 未知口径 fail-closed）、1455 委托等价、**耦合恒等式作数值不变量钉死**（未来任何「把 est_chg 塞回裁决分母」的改动必须先撞穿此测例）、两契约一致性黄金（est_chg 恰为 T 日真实涨跌 ⇒ 核算 label 退化为裁决 label）。
- **lookthrough 一行修复**（`core/lookthrough.py` `evaluate_lookthrough`）：`history[-1]` → `effective_snapshot(history, 今日)` + None 守卫。回测侧（`backtest_spread`）一直用 `effective_snapshot` 防前视，唯独报告侧取最新期——披露滞后窗内（年报最长 95 天）最新一期「尚未可知」，取了即实时报告前视；无生效期 → 跳过，由 run.py `lt_missing` 探针显式降级，不造数。新增 `tests/test_lookthrough_pit.py` 3 项（fast 层全 mock 零网络：未生效期取最近生效期 / 已生效期行为兼容 / 无生效期诚实跳过）。
- **测试与验证**：`.venv` fast **499→511**（净 +12）、slow **158 OK (skipped=3)**，零回归；未动模型权重 / 门禁 / `model_ready` / 冻结件 / 训练，可回滚。
- **对 09-26 议程的影响**：「1455 口径去留拍板」已按 (a) 落成合同文本 `forecast-v1`（1455 = 决策输入 + 盈亏核算；预测/裁决一律 old 口径）——若未来选 (b)/(c) 须重开契约版本（forecast-v2），合同文本明示事后补无效，09-26 只余确认不重开。

---

## 五-F、V4.4-附：生产停摆事故与修复（2026-09-23 16:30，commits `5b91bf7` / `0db6302`）

**事故**：今日 `QuantFund_Mid` / `QuantFund_Post` 两槽 **Last Result = 1**，日志停在 `[stat]` 之后、`[repo]` 之前——报告未落盘、飞书未推、今日 `run_manifest` 缺失、Obsidian 信号流水无今日行。14:55 的数据采集（intraday 特征 / 市场上下文 / state-ref）**在崩溃前已落盘**，证据链未断，断的是产出链。

**根因（一句话）**：`672b9ed`（09-22 16:07 评审修复批）把门禁评估前移，给 `generate_report` 与 `push_feishu` 都传了 `gate=`，但**两个函数签名从未接收该参数** ⇒ 每次运行必 `TypeError`，生产链路 100% 崩溃。

**为什么 511 项全绿没拦住**：fast 层测例全部用**旧签名直调**这两个函数，没有任何一处经过「run.py 的真实调用形状」。这类**调用方与被调方签名漂移**是纯静态错位，跑得再多单测也不暴露——必须专门守护。

**修法（两处都补齐，不删调用方——门禁前移本就是 V4-A 的设计意图）**：
1. `core/report_generator.py`：签名加 `gate: dict | None = None`，新增 `gate_section(gate)` 在报告尾部插「🚦 发布资格门禁」段；判据与措辞取 `publish_gate` 的 `detail`（单一真源），本函数只呈现、不另起判定；`superseded`（早轮脏、末轮已恢复）按「仅观测」措辞标注。
2. `core/notify.py`：`push_feishu` / `_build_card` 加 `gate=`，新增 `gate_note(gate)` 在卡片免责脚注**之前**插一行门禁状态。run.py 只在 `gate.ok=True` 分支才推送，所以线上这张卡恒为「✅」——价值是让 Summer 在手机上直接看到这轮靠哪几条证据放行，不必回头翻落盘报告。
3. 两处 gate 缺省一律出空串 ⇒ **旧调用方行为逐字节不变**（报告主体条件拼接，不多插空元素）。

**防复发（本批最重要的产出）**：`tests/test_publish_gate.py::TestRunWiring::test_gate_kwargs_match_callee_signatures`——用 AST 扫 `run.py` 对这两个函数的实调用，断言**传出的关键字参数 ⊆ 被调函数签名参数集**（无 `**kwargs` 兜底时多一个键当场红），并断言两处调用本身仍在场（防守护空转）。另加 3 项展示契约测例（报告段含 verdict 与 superseded 观测 / 卡片行插在脚注前且脚注仍在最后 / 未配 webhook 走降级分支不抛 TypeError，显式置空环境变量保证零网络）。

**验证与留痕**：`.venv` fast **511→515 OK**、slow **158 OK (skipped=3)**，零回归；全程零东财请求，未动模型权重 / 门禁判据 / 冻结件；`cc20c8a..0db6302` 推 origin/main（修复笔 `5b91bf7` + 数据留档笔 `0db6302` 分开提交）。另做一次全仓 AST 关键字错位审计，除本处外**无第二处真实错位**（其余告警均为同名函数误报）。

**顺带：quantile calib 基线在 0910 冻结件上重出**（零网络，20s；脚本默认自动选件，实测 auto = `samples_frozen_20260910.jsonl`，G-A PASS / G-B DRIFTED，train < 2025-05-14 = 2438 / OOS = 923）：

| horizon | Coverage80 原始 | 95% CI | Pinball 技能(Σ) | Q50 RankIC | CQR 外扩后 | qhat（4 折） |
|:--|--:|:--|--:|:--|--:|--:|
| T+1 | 59.4% | [56.4%, 62.4%] | +0.00081 | −0.001 [−0.063,+0.062] | **76.3%** 入带宽 | 0.0114 |
| T+3 | 55.7% | [52.3%, 59.0%] | +0.00114 | −0.014 [−0.076,+0.053] | **74.2%** 入带宽 | 0.0206 |
| T+5 | 51.9% | [48.6%, 54.5%] | +0.00160 | −0.028 [−0.084,+0.024] | **73.8%** 入带宽 | 0.0275 |

- 三周期原始 coverage 全部 **<0.70 ⇒ 区间过窄**（风险被低估，与 09-01 定性一致）；CQR 常数外扩后全部回到预注册带宽 70%~90% 内。
- 路径层（仅 T+5，分布级粗校准，观察层）：真 mdd5 ≤ mdd_q10 命中 **4.2%**（期望 ~10%）/ ≤ mdd_q50 **26.8%**（期望 ~50%）/ mfe5 ≤ mfe_q50 **35.3%**（期望 ~50%）⇒ 尾部与中位双双低估。
- ⚠️ **口径纪律**：G-B DRIFTED ⇒ 本轮绝对值与 09-01 锚定轮次**不可直接比**（只可作趋势参照）；本报告仍是**证据层诊断**，不绑 registry / promotion，`model_ready` 不受影响。
- 落档：`output/backtest_quantile_calib_20260923.md`（`output/*.md` 被 gitignore，本地留档）。

**待 Summer 定夺（不属修复本身，需授权）**：今日两槽**报告与飞书卡片已永久缺失**（14:55 实时快照无法重现）。是否补跑 `run.py --slot post` 重新生成今日报告？——补跑会**重新发东财/腾讯请求**（过铁律 7 四闸门 + 需显式授权），且报告时点与 est_chg 数值将是「补跑时刻」而非 14:55，属**近似件**；明日 11:30 起链路自动恢复正常。建议：不补近似件，留此事故记录即可，明晨正常跟。

---

## 五-G、V4.4-债务批：EOD_PROXY 诚实标注 + 裁决 label 收归唯一真源（2026-09-23 17:30，commit `f407879`）

**背景**：GPT 对仓库的深度审阅中最实的一条——「历史上模型看到的，是不是它上线时真能看到的东西？」核实属实：`backtest_spread.py:172` 用 `bisect_right(dates, d) - 1` 取到 **d 日收盘价**当 14:55 价（源码注释原作“仅差尾盘漂移”）。同批还把 P0-2（label 双实现）一并还债。

### ① EOD_PROXY 口径诚实化（数值层零变化）

| 项 | 内容 |
|---|---|
| 病灶 | 历史样本的个股估涨用 **当日 EOD close** 近似 14:55 价，不是真 14:55 快照 |
| 残差量级 | **有界**——只差「14:55→收盘」尾盘漂移，不是跳日错位 |
| 不在此口径列 | NAV 系因子（`_nav_state_at` 已 PIT 错位至 T-1）；持仓披露滞后（`_QTR_LAG`，`check_pit()` 独立审计） |
| 代码落点 | `backtest_spread.HISTORICAL_FEATURE_MODE = "EOD_PROXY"` + `HISTORICAL_FEATURE_MODES` 枚举 |
| 落盘落点 | `freeze_samples.py` meta 新增 `historical_feature_mode`，值从**生产真源常量读取**（`_hist_mode()`，防两处漂移） |
| 冻结件纪律 | 0910 冻结件 **本体与 meta 一律不追溯改写**（冻结即冻结，改它等于伪造冻结事实） |
| 文档 | README 新增「历史特征时点口径声明」表段 |

### ② 裁决 label 收归唯一真源

`backtest_spread.py` 的 `FWD_LIST` / `EXTRA_FWD` 两处自算 `navs[i+h]/navs[i]-1` 改调 `core.forecast_contract.adjudication_label`（forecast-v1 契约 C1）。

- **语义等价实证**：改动前后各重建一次样本，**3407 行逐行哈希逐位一致**（`sha256=0e452da18f26f589318ba990fc96e15d1a6b012ef681207762c9706108bc992f`）。
- **边界差异（有意）**：`nav_t ≤ 0` 或 `None` ⇒ 返回 `None`（原式会产出 inf/NaN）⇒ 该 label 不可算、不做保留判定，**不造数**。基金 NAV 正常恒正，实际路径不可能触发。
- `mdd*/mfe*`（路径统计，非裁决 label）**不纳入本次改动**。

### 护栏与验证

- 新增 **8 条**测例（`test_forecast_contract.py`）：AST 钉死 `backtest_spread` 不得回退自算 label（含 `navs[...]` 下标不得出现在除法表达式中）；`EOD_PROXY` 常量 + 实现层 `bisect_right(dates, d) - 1` 钉死（真换 14:55 快照时强制同步改口径常量）；0910 meta 不得被追溯加字段；枚举须能表达 `PIT_1455_SNAPSHOT`。
- 全量 **678 项**全过（较前 +8，含新增护栏）；`backtest_spread.py` 实跑消费冻结件端到端正常（方向差原版 +0.97% / 去缠论 +1.13% / 加权 +1.60%，与既有一致）。
- 零网络、未动模型权重 / 门禁判据 / 冻结件本体 / `model_ready`（仍 false）。

**仍待补的真缺口（不变）**：`data/` 下无历史 14:55 行情快照 ⇒ 历史时点一致性**无法真正重建**，只能标注。今日起每槽落盘攒训练轨；东财/腾讯历史分钟线回补**需 Summer 单独授权**。

> 📄 **回补探测方案已起草（2026-09-23，零执行）**：`QuantV1/output/ops_runs/2026-09-23-probe-intraday-backfill-plan.md`
> ——缺口定义（`data/intraday/` 22 个文件全为 `{date}_mid.json`，无任何 `_post.json`；`run.py:465-466` 仅 mid 落盘；`core/intraday_store.py:27-29` 只留最近 30 个文件）、
> 回补目标刻画（**唯一外部依赖 = 每只重仓股在历史交易日「截至 14:55 涨跌幅」**，非基金分钟线；同一数据源可顺带回补 11:30 槽）、
> 七条可判定问题（端点可用性/深度、14:55 bar 时间戳语义、复权行为、双源一致性、真 14:55 vs 日线近似误差、全量请求量）、
> G0（零网络可先行）/ G1 G2 G3（**须 Summer 显式授权 + 铁律 7 四项闸门**）/ G4 步骤。**方案≠执行**，G1~G3 一发请求都未发。
> README 侧指针已同步挂在「历史特征时点口径声明（EOD_PROXY）」节。

---

## 五-H、V4.4-债务批 ③：early_stopping A/B/C 对照实验（2026-09-23 18:00，commit `92049d7`）

**问题**：生产 `core/forecast_engine.py` 三处 HistGB 全部 `early_stopping=True, random_state=42` ⇒ sklearn 内部做**随机** `train_test_split` 切验证集（sklearn 1.9 `gradient_boosting.py:556`）。时间序列任务里随机切分 = 验证集含训练集未来样本 ⇒ 早停判据本身可能被泄漏污染。`random_state=42` 固定 ⇒ 这是**方法学纯度**问题，不是可复现性问题（修正了 GPT 审阅的措辞）。

### 三轨设计（同一 0910 冻结件、同特征、同 OOS 切分，只改早停策略）

| 轨 | 策略 |
|---|---|
| **A** | 现状复刻：`early_stopping=True`（内部随机 10% 验证集） |
| **B** | 早停关闭：`early_stopping=False`（跑满 `max_iter=200`） |
| **C** | 显式时间序验证集：训练段**末尾**切 15% 传 `fit(X_val=…)` ⇒ 跳过内部随机切分 |

### 结果（OOS 段 1711 行，training 1660 行 < 2023-09-21）

| 周期 | A n_iter | A RankIC | B n_iter | B RankIC | C n_iter | C RankIC | **Δ(C−A)** |
|:--|--:|--:|--:|--:|--:|--:|--:|
| T+1 | 29 | +0.0166 | 200 | +0.0092 | 15 | −0.0561 | **−0.0727** |
| T+3 | 45 | −0.0248 | 200 | +0.0077 | 16 | −0.1242 | **−0.0994** |
| T+5 | 23 | +0.1064 | 200 | +0.0889 | 14 | +0.0200 | **−0.0864** |

### 稳健性（决定性的那一步）

val_frac 扫描 0.10 / 0.15 / 0.20 / 0.30 × 3 周期，**12/12 组合全部 C<A**，且 Δ 随窗口增大而恶化：

| val_frac | T+1 ΔIC | T+3 ΔIC | T+5 ΔIC |
|:--|--:|--:|--:|
| 0.10 | −0.0456 | −0.0288 | −0.0769 |
| 0.15 | −0.0727 | −0.0994 | −0.0863 |
| 0.20 | −0.0812 | −0.0793 | −0.1121 |
| 0.30 | −0.0725 | −0.0749 | −0.1441 |

⇒ **不是单窗口偶然，是时间序验证的系统性选择偏差**：时间序验证集看到的是**旧 regime**，早停停在了**欠拟合**位置（C 轨只跑 14~16 步）。

### 结论（反直觉但证据硬）

**A 轨的随机切分虽有纯度瑕疵，但其验证集≈全体数据分布，反而选到了更合适的迭代数。**

- 生产策略**维持不变**（不改 `forecast_engine`），本实验作为**已知证据**留档——不把纯度瑕疵当缺陷追修。
- 这意味着 GPT 审阅中「early_stopping 是方法学污染」的指控**方向对、后果反**：切分确实非因果，但它在这份数据上不是降低了性能，而是**抬高了**（相对严格时间序验证）。
- 真正的开放风险仍在别处：G-B DRIFTED ⇒ 绝对值不可与锚定轮次直接比，**该实验只作轨间相对对照**，不作绝对值门槛。

### 印证

- `backtest_early_stopping_ab.py`（344 行，零网络 socket 守卫）；不改任何生产脚本 / 不进 registry / 不动门禁 / `model_ready` 仍 false。
- 报告 → `output/backtest_early_stopping_ab_20260923.md`（gitignore）；机读 → `forecast_outputs/early_stopping_ab_20260923.json`。
- 全量 **678 项**单测通过，零回归。

---

## 五-I、B 契约批：Forecast 晋升契约 B（B1/B2/B3）落地（2026-09-23 深夜，commit `35913a1`）

**来源**：GPT 外部评审四红项（OOS 反复复用 / promotion 只看 pooled / action bootstrap 非聚类 / fwd20 连坐）+ Summer 拍板**路线 A**（decision_edge 正式进入 promotion 判据）。本批只做契约固化的三项工程改动：FINAL LOCKBOX 不执行、不发任何外部请求、`model_ready=false` 与全部门禁一字不动。

- **契约落档**：`evidence/contracts/forecast_promotion_contract_B_20260923.md`——四基金泛化 + baseline edge 正式晋升契约（草案/待预注册）：§5.1 晋升需 `decision_edge_CI_lower > 0`（仅 RankIC(model)>RankIC(est_chg) 不再充分）；§6 四基金逐基金 fund×horizon 证据矩阵，小样本标 `INSUFFICIENT_POWER` 且整体 BLOCKED（专防 025687 n=35 被迫二元结论）；§8 统一日聚类 bootstrap；§10 **0910 OOS 永久降级为 development/selection evidence**；§11 promotion 状态机（BLOCKED_POWER/BASELINE_EDGE/CALIBRATION/PROVENANCE…）；§6.2 `power_threshold` 留白，FINAL LOCKBOX 前功效分析冻结。
- **B1 action CI 统一聚类**：`backtest_action.bootstrap_ci` 改复用 `backtest_forecast.cluster_bootstrap_ci`（按交易日整块重抽，唯一实现禁止另造）；交易日 <10 → CI 记 n/a，「CI 下界 > 0」判定自然 fail-closed（不再伪造 0.0）。测例 `tests/test_action_bootstrap.py` 4 项（与唯一实现同参同结果 / 聚类 CI 显著宽于独立假设 / 不足 fail-closed / nan 判定与报告 n/a 如实标注）。
- **B2 协议 exact 校验**：`verify_feature_protocol` 从 keys/dim/enabled 三项升级为 protocol_version / feature_keys / n_features / feature_dim / masking 整块（enabled+layout+missing_value+mask_value）逐字段 exact——布局翻转（value_then_mask→mask_then_value）或填充值漂移在维度不变时不再漏检。已现场核实 `forecast_engine.MASKING_PROTOCOL` 与 `B1_MASKING_PROTOCOL` 四键逐字段一致，v3 加载路径不受影响。测例 `tests/test_feature_protocol_strict.py` 8 向正反（含「维度伪装回 14、只翻掩码语义」真实攻击面）。
- **B3 短周期样本解耦**：`load_samples` 新增仅关键字参数 `require_fwds`（默认 `FWD_LIST=(5,10,20)`，既有调用方与冻结件口径逐位不变）；`backtest_forecast` / `train_forecast_model` 入口改 `require_fwds=()` 保留全部特征行，fwd1/3/5 标签由 `build_xy` 按 horizon 逐行过滤——时间末端约 20 个交易日的样本不再被 fwd20 连坐丢弃；`excess10` 对缺 fwd10 行安全跳过（不报错不造数），既有完整样本数值不变。测例 `tests/test_b3_sample_retention.py` 6 项（签名契约 / 保留判定 / 独立可用 / 安全跳过 / 数值不变 / 源码接线断言）。
- **验证与留痕**：三份新测例登记 `tests/layers.py` fast 层（守护测例要求全登记）；全量 **52 文件 699 测 OK**（`run_tests.py --all`），零回归。运行留档（21:30 补拉 skip=4 / 22:30 Shadow 全 HOLD）另起 `1d3d994`。两笔均已推送 origin/main。
- **B 批之后**：按契约 §10，FINAL LOCKBOX 等 C 批（真实 14:55 历史样本，G1~G3 需 Summer 显式授权）+ 功效阈值冻结后**一次生成**；当前 0910 OOS 自本批起只作 DEV/SELECTION 证据使用。

---

## 六、开放问题与决策账（已结案）
- ✅ **label 分母**：从 14:55 可观测状态起算，全量重算（本批已落契约 + 数据集）。
- ✅ **SSOT 指针**：新写 09-22 篇（= 本篇），接替 0902。
- ✅ **P0 四项（D0 批，2026-09-23）**：decision_edge 两轨落地 / 校准口径复查（无分叉，不改）/ Obsidian 路径配置化 / registry 门禁复算（全一致；P1-2 FAIL 为既有状态，处置方向不变）。详见五-D。
- ✅ **Forecast Contract 重定（2026-09-23，Summer 批准）**：按 (a) 落成 `forecast-v1`——裁决 label = T 日最终 NAV 口径（生产公式一字未变）、est_chg 不进 label、1455 降级为「决策输入 + 盈亏核算」、裁决基线写死 est_chg 单因子；合同测试 9 项 + lookthrough 报告侧 PIT 一行修复 3 项同批落地。详见五-E。
- ✅ **B 契约路线拍板（2026-09-23，Summer 拍板路线 A）**：decision_edge 正式进入 promotion 判据（`decision_edge_CI_lower > 0`）+ 四基金泛化证据矩阵 + 0910 OOS 降级 DEV/SELECTION；B1/B2/B3 工程改动同日落地（`35913a1`）。`derive_promotion` 读取新 schema 属后续独立批次（schema 字段尚未由验证器产出）。详见五-I。
- ✅ **下一步优先级 A/B/C 之择**：确认「先修证据链」，但执行序按「零网络可回滚优先」= 先 1（契约+量纲桥）→ 2（重算构建器）→ 3（验收矩阵）→ 4（SSOT）。1、2、4 已完成；证据链的"重训+重跑验证报告"依赖量纲接入，排步 2 之后。

---

## 七、下一步落点（按优先级）

- ✅ **V4.4-重定批（2026-09-23，Summer 批准）**：Forecast Contract `forecast-v1` 权威文本（C1–C5）+ contract test 骨架（9 项）+ lookthrough 报告侧 PIT 一行修复（3 项），`a73f1ed` / `040c26e`，详见五-E。fast 499→511 / slow 158 OK 零回归。
- ✅ **P0 批（D0，2026-09-23）**：四项零网络修/验证完成（两轨 decision_edge / 校准口径复查 / Obsidian 路径配置化 / registry 门禁复算），`ef6d7c1` / `5925a3e`，详见五-D。
1. ✅ **步 1**：14:55 契约 + est_chg 量纲桥登记 + 重算构建器（已完成，`e44af41`）。
2. ✅ **步 2**：**量纲接入**（2026-09-22 晚 Summer 授权执行，commit `0ba2733`）——run.py build_forecast_meta / decision_engine fallback 统一过 `est_chg_from_pct()`；store 只追加不覆写 ⇒ 契约新增切换日 `EST_CHG_LIVE_FRACTION_SINCE="2026-09-23"`，shadow / score_expert_a 读取端按行日期判别（旧行过桥、新行透传）；shadow 镜像分走逆向桥还原 % 域并留 `est_chg_fraction` 原值。**裁量：不 bump MODEL_VERSION、不重训换权重**——训练样本本就是 fraction（错在 live 输入侧），且 prereg 授权把 v3 sha 钉死至 10-12，换权重即断今晚 shadow 降级授权；「重训 v4」留待拍板。验证重跑消费 0910 冻结件（G-A PASS / G-B DRIFTED），两次运行字节级一致，T+1/3/5 全 rejected（rank_ic +0.049/+0.048/−0.019 CI 跨零）→ 维持 model_ready=false；报告 `output/backtest_forecast_v44_step2_20260922.log` 仅留档未 bind_validation（护住 prereg D3，v3 现行授权不受影响）。全程零东财请求（冻结快照 + 本地指纹重算），铁律 7/8 以「无请求」满足。fast 471→477、slow 158 OK。
3. ✅ **步 3**：**四基金 × 多周期验收矩阵**（2026-09-22 晚完成，零网络，commit `b1fcd22`）—— 详见「五-B」节。要点：① C 轨自校验复现步 2（|Δ|≤5e-4）⇒ 指标实现与生产验证器等价；② 🔴 发现 1455 口径与输入特征 `est_chg` **机械耦合**，诚实基线须从 0 换成 `−est_chg`（池级基线 IC +0.1278/+0.0393/+0.0567）⇒ 9 格仅 1 格（C 轨 T+3 超额 +0.0203）跑赢基线，**「1455 口径下模型有排序能力」不成立**；③ 校准回答：现行模型 mean p_up 系统性**低估**上涨（偏差 −0.059/−0.036/−0.036），非高估；④ G-B DRIFTED 经量化复核为**净值层零漂移**，新旧对照是纯口径差。测试 fast 474→488、slow 158 = 646 OK，零回归；未动任何门禁。
4. ✅ **步 4**：SSOT 新篇 + 总览索引指针切换（本篇 + 索引 39/50 行改指 0922）。
5. 观察层样本积累（market_context / intraday delta）与 09-26 阶段性复核继续，均**≠ 晋升评估**；**09-26 附加议程**：① 验证 ❌ 与 09-08 `--fresh` 历史报告的口径差复核（冻结 vs 活拉不可比）；② 「是否重训 v4」拍板（需同步改 prereg 授权 + 新冻结件重训 + bind_validation 一次到位）；③ **1455 口径去留**：已按 (a) 落成 `forecast-v1`（2026-09-23，见五-E）——1455 = 决策输入 + 盈亏核算、预测/裁决一律 old 口径、基线写死 est_chg 单因子；未来若改选 (b)/(c) 须重开契约版本（事后补无效），09-26 只余确认不重开。

---

## 八、相关笔记

- [[基金日频参谋-当前状态与下一步-20260902]] — 已被本篇接替，转历史存档
- [[量化交易工具-总览索引]] — 导航索引（SSOT 指针已切 0922）
- [[C2生产轨三拆与特征非不变性-20260918]] — est_chg 特征非时点不变性（量纲 P0 的前序线索）
- [[冻结输入三件套锁定-D3与施工事故-20260918]] — 冻结件三件套口径

---

*本篇由 OpenSquilla 于 2026-09-22 创建，接替 0902 篇成为 SSOT。*

---

## 五-J、A 批：验证证据血统三重对账 + stock cache 收口 + SSOT 审计指针（2026-09-24）

**路线**：Summer 已确认「A 先于 B-G0」。本批严格遵守边界：不改 `derive_promotion` 五门判据、不改模型权重、不训练、不请求东财/腾讯、不执行历史 14:55 回补。

### ① A-1：validation provenance 三重等式

在 `core/model_registry.py` 的 `bind_validation()` I/O 边界新增 fail-closed 对账函数：

- `artifact_sha256` = registry 当前 `models[pkl].sha256`
- `dataset_sha256` = registry 当前 `snapshot_provenance.samples_sha256_lf`
- `git_commit` = registry 当前模型登记的训练代码锚点 `git_commit`

缺任一项、registry 血统缺失、任一不等式成立 ⇒ **拒绝绑定**。对账发生在 bind/I/O 边界，`derive_promotion()` 纯函数判据不读取磁盘、不改规则。

`validation_schema.PROVENANCE_KEYS` 已增加 `artifact_sha256`；`backtest_forecast.assemble_validation_evidence()` 透传该字段。当前独立回测若拿不到实际 artifact sha，诚实落 `UNKNOWN`；因此这类报告不能冒充可绑定证据。

### ② A-2：反例测例

`tests/test_model_registry.py` 新增血统反例矩阵：

- 模型 A + 报告 A → PASS
- 模型 B + 报告 A → FAIL（artifact mismatch）
- 数据集 A + 报告 B → FAIL（dataset mismatch）
- 代码 A + 报告 B → FAIL（git_commit mismatch）
- provenance 缺失 → FAIL

这正好补上原先「报告 sha 防替换」之外的**声明内容 ↔ registry 实况**第二道门。

### ③ P1-B：stock cache 小尾巴

`core/stock_data.py`：
- TTL 内坏 JSON / 非预期 cache payload 不再直接抛出，改为继续走现有 provider chain；
- provider 成功后的 cache 改为 `tmp + os.replace` 原子写；
- 新增 `tests/test_stock_data_cache.py` 两项，零网络：坏缓存回退 provider、原子写无残留 tmp。

**复用现有存量模式**，没有新增缓存体系。

### ④ P1-C：SSOT stale 检测机制

新增 `evidence/ssot_git_tip.txt` 作为 SSOT 收口指针；`audit_project.py` 新增 `P1-12 SSOT git tip 对账`：

- 正常闭合：指针值 = 当前闭合提交的 `HEAD^`；
- 若代码在 SSOT 收口后继续产生提交：检查变为 WARN，明确提示「SSOT 可能落后当前代码」；
- 查不到 Git / 指针缺失：WARN，不把未知当 PASS。

本批最终 QuantProject tip = **`b38249f`**；SSOT 已同步到该 tip。

### ⑤ 本批未执行项

- ❌ 全量 `.venv` 测试：当前 GitHub 连接环境无法运行用户 Windows 本地 `.venv`；不伪造测试结果。
- ❌ B-G0：下一批，只做零网络只读探测；G1~G3 仍需 Summer 显式授权。
- ❌ LOFO consumption gate：继续排在 A 后、`model_ready` 打开前。
- ❌ FINAL LOCKBOX / model_ready / history_validated / MODEL_VERSION bump：均未触碰。

### ⑥ 当前 SSOT 结论

**A 已落地；B-G0 为下一批。**

09-26 硬节点的季度证据重估基线仍以现有真实留档为准；本批没有借修复动作重新解释 0910 数字，也没有把 G-B DRIFTED 证据升级成 FINAL LOCKBOX。