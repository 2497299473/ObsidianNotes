---
title: GPT审查落地-阶段0止血
created: 2026-09-11
tags:
  - 量化交易
  - 代码审查
  - 证据链
  - shadow
description: 09-11 GPT 项目审查报告 → 阶段 0「立即止血」5 项的落地核对、独立验证、legacy_invalid 标记与定时任务影响；Tushare HTTPS 一项未做
---

# GPT 审查落地：阶段 0 止血（2026-09-11 夜）

> 触发：Summer 让 GPT 审查 QuantV1 并改代码，请 OpenSquilla 复核「改得对不对、是否影响全部定时任务」。
> 报告：`D:\PythonProject\QuantV1\outputs\QuantV1-项目审查-20260911.html`（8 大问题 + 5 类 30 个可测试断言 + 阶段 0–5 路线图）。
> 结论先行：**GPT 改动方向正确、实现质量高、256 测试全过，不破坏任何定时任务；阶段 0 五项落地 4 项，HTTPS token 未做，历史 shadow 的 legacy_invalid 标记由 OpenSquilla 当晚补完**。

---

## 一、阶段 0 清单 vs 实际落地

| 审查「立即止血」项 | 状态 | 落点 |
|---|---|---|
| 修正 `_merged` 顺序（14:55 优先） | ✅ | `core/decision_engine.py` 循环序 `(1455,1130)`→`(1130,1455)` + 回归测试 |
| 停用当前 shadow 作为晋升证据 | ✅（补完） | `shadow_policy.py` 改消费当日 post 槽真实冻结特征、未批准即拒记；**36 条历史样本全量标 `legacy_invalid`**（OpenSquilla 22:09 补） |
| 修 bootstrap 日期错位 | ✅ | `build_xy()` → `XYBatch`（X/y 与 date/fund 同行生成），6 个回测脚本改用 `XY.dates` |
| 模型先验 hash 再反序列化 | ✅ | `model_registry.read_verified_model_bytes()`：原始 bytes 过 sha256 才 `pickle.loads`，测试断言 hash 失败时 loads 不被调用 |
| HTTPS token（Tushare） | ❌ 未做 | `core/stock_data.py:66` 仍 `http://api.tushare.pro`，**遗留** |

超出报告的增量：`verify_approval()` 还把 **validation 报告文件 hash** 也纳入授权链（防报告与模型脱钩），`model_ready`（config 意图）与 `model_approved`（registry 原子授权）分离——单改 config 布尔再也解不开对外展示。

## 二、独立验证（OpenSquilla）

1. **测试**：项目 venv `python -m unittest discover -s tests` → **Ran 256 tests, OK (skipped=3)**（审查时 249，净增 7 条回归测试）。
2. **门禁链现状**：registry 两模型（v2/v3）均 `validation=rejected` + `promotion=blocked`，config `model_ready=false` → 改后对外预测输出与改前**完全一致**（占位 + HOLD 纪律），修复不改变当前产品行为，只改变「未来误配置时不再装作可用」。
3. **时间线**：代码改动 mtime **18:05–18:09**，晚于当日 14:55 生产运行 → 当天各时点均为旧代码；**下周一 2026-09-14 起新代码生效**。

## 三、legacy_invalid 标记（当晚补完）

- 对象：`output/shadow_actions.jsonl` 全部 36 条（`date` 2026-08-03 ~ 08-13，事后重放生成、特征含已实现净值 = 未来标签）。
- 做法：逐条**追加** `evidence_validity="legacy_invalid"` + `invalidated_at` + `invalidated_reason` 三字段，原行内容不动（diff 36+/36-）。
- 备份：`backups/shadow_actions_pre_legacy_invalid_20260911.jsonl`。
- 下游核查：全项目仅 `shadow_policy.py` 写入该文件，无 scorecard/晋升代码读取它 → 标记为纯防御性留痕，零破坏（tests 亦不读真实文件）。

## 四、对定时任务的影响（全量盘点）

- **Windows 计划任务**：QuantFund_Mid 11:30 / Post 14:55 / KlineEvening 21:30（均 `run.py`）→ 不受破坏；14:55 起 merged 字段正确以尾盘为准（修复目的），动作层仍被 `history_validated` 锁死。
- **OpenSquilla cron**：16:00 板块K线 / 09:30 晨检 / 23:00 日汇总 / 09-26 季度重估 → 零涉及。
- ⚠️ **预期变化（别当故障）**：22:30 Shadow 任务在模型未获完整批准前会稳定记录「拒绝记录、退出码 1」——这正是审查要求的「停用 shadow 作为晋升证据」，run.py 内调用容错不阻断主流程。

## 五、Git 落地

- `ed9e0d3` 审查阶段 0 止血（16 文件：9 源码 + 7 测试，+327/−90）
- `c16e067` 证据链：历史 shadow 36/36 标 legacy_invalid
- 未入库（按项目证据链纪律可单独提交）：当日运行产物 `data/intraday_features.jsonl`、`data/market_context/2026-09-11.json`、`output/state_ref_history.jsonl`、`output/zcode_runs/2026-09-11.md`

## 六、遗留 → 下一步

1. **Tushare token 改 HTTPS**（一行，阶段 0 收尾）。
2. 阶段 1 证据底座：不可变原始数据层、experiment/run manifest、knowledge_at。
3. 前瞻性 shadow 连续计数从 09-14 重新起算（目标 ≥60 有效交易日，审查阶段 2 标准）。
4. 审查 HTML 中「60 交易日从 09-02/09-07 起算」的说法已过时：那两天仍是历史回放样本，且 09-10 起模型被 config 主动禁用——真正干净的 prospective 数据要从新 shadow 路径上线日算起。
