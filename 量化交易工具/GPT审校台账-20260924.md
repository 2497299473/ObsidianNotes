# GPT 审校台账 · 2026-09-24（第 1 轮，已闭环）

- 流程依据：`D:\PythonProject\GPT-协作公约.md` v1.1 + `D:\PythonProject\GPT-审校台账-回帖格式.md` v1
- 角色：B = ChatGPT 网页版（GPT-5.6 Luna · 高强度推理档 · 知识截止 2025-12，自报）；A = OpenSquilla 执行方；Summer = 仲裁
- 议题：`924a5a1`（A 批修复：FRESH 语义前移 + artifact_sha256 槽位）是真清账还是掩盖病症
- **状态：第 1 轮闭环，R1 全部条目终态见下表；无遗留争议**

## 轮次时间线

| 拍 | 内容 |
|---|---|
| B-Round1 | 7 条意见（0 P0 / 3 P1 / 2 P2），行号引用 7/7 属实，无谄媚无越权 |
| A-回应1 | 同意 5 / 待定 2 / 反对 0；B 的测例 A 代跑通过；发现 b49e125 基线漂移 + f9b1495 未推送 |
| B-Round2 | 6 条（R1-8~13）：接受 A 全部反馈，主动认 diff 格式瑕疵；要求基准迁移到分支头 |
| A-回应2 | 同意 6 / 反对 0；分支头两模块实跑全绿；更正 B 拓扑表述（924a5a1 才是分叉点）——A 当时报的“44 提交”系目测误数，后经 rev-list 复核为 core/tests 42 / 全区间 70 |
| Summer | 三事项裁决「123 按共识」 |
| A-落地 | `230e0a3` 推送 v4.4-a-final-1d（全量 842 测绿，skipped=3） |
| B-预审 | 自标 Round 2，基于 0388c21 旧快照（B 读仓时 230e0a3 尚未推送）：R2-1~7；70/64 属实、拓扑精化正确；A 认 44→42 误数 |
| A-回应3 | R2-1 自设条件触发 → R1-3/R1-7 CLOSED；口径认账；第 2 轮基准 230e0a3 不变 |

## R 编号对账表（终态）

| R# | 要点 | 级别 | 终态 | 落点 |
|---|---|---|---|---|
| R1-1 | 五键门未被 924a5a1 改动，无 P0 | — | ✅ 共识 | 审校确认 |
| R1-2 | 无可证明的 FRESH 放行路径 | — | ✅ 共识 | 审校确认 |
| R1-3 | FRESH 测例改打 bind 层，下游门保护变薄 | P1 | ✅ 落地 | `230e0a3` 新测例 |
| R1-4 | bind 丢弃 provenance 具体原因 | P1 | 🕓 待定 | 并入第 2 轮（R1-12 同） |
| R1-5 | UNKNOWN 易被误读为可晋升 | P1 | ✅ 落地 | `230e0a3` docstring 钉死双层语义 |
| R1-6 | provenance 显式状态机 | P2 | 🕓 待定 | 复查 2026-10-08（V4 重构后） |
| R1-7 | 绕过 bind 的对抗测例 | P2 | ✅ 落地 | `230e0a3`（与 R1-3 同测例） |
| R1-8 | b49e125 未放行 FRESH，反而加码（KFP 门 + A4 复核） | — | ✅ 共识 | A 全文核实 |
| R1-9 | 测例必须落库才算闭环 | P1 | ✅ 落地 | `230e0a3` |
| R1-10 | 基准迁移到实际交付头 | P1 | ✅ 落地 | 第 2 轮基准 = 230e0a3 |
| R1-11 | 钉措辞、不加第二套授权判断 | P1 | ✅ 落地 | 纯注释零逻辑改动 |
| R1-12 | 先审 A4 再定 reason 结构化 | P2 | 🕓 并入第 2 轮 | — |
| R1-13 | 穿透链固定：artifact/report → validation.provenance → schema v2 → derive_promotion 五门 → verify_approval → shadow prereg 旁路 | P2 | ✅ 采纳 | 第 2 轮议题框架 |

## 第 2 轮（正式）交接参数

- **基准 sha：`230e0a34e7eabbf430b6be172ee61d00578e2177` · 分支 `v4.4-a-final-1d`**（已推送，B 可直接读）
- 议题：evidence → promotion 五门穿透审计（R1-13 链，分支头真实门梯：verify_model → 特征协议 → validation 报告 → promotion → derive_promotion 五门 → v2 PIT/KFP 硬门 → 五键门 → registry KFP 门 → A4 → config.model_ready AND，另 shadow prereg 旁路）
- B 上轮引用（model_registry.py:287-292）在分支头已失效，五键门现位于 304-310（`230e0a3` 前为 0388c21 版行号）
- 审计区间口径（B-R2-2 收紧，A 认账）：`924a5a1..230e0a3` **全区间 72 commits**（core/tests 子集 43），非 A-final-1 命名的提交一并审

## 流程压测结论（首轮有效性）

1. **B 可用**：静态审校质量高（行号 7/7 属实、P0 判断正确、接口符号零臆猜、被指出格式错误即认账）；Luna 档够用，暂不必升 Sol。
2. **互补结构成立**：B 负责跨文件契约/失败路径追踪；A 负责运行验证（B 跑不了测试）、远端 tip 权威（ls-remote）、拓扑/门梯核实。A 两轮共发现 B 未提事实 5 项（b49e125 漂移、f9b1495 未推送、shadow 旁路、44 提交偏离幅度、A4 门序保持拒绝原因）。
3. **防幻觉机制有效**：B 全程自标「未运行验证」，A 代跑闭环；A 全部结论附命令与输出。
4. 教训固化：审校基准必须显式带分支（已写入公约 v1.1 §11）。

## 未决 / 风险提示

- R1-4 + R1-12（bind 失败原因可见性）→ 第 2 轮设计点。
- main 与 1d 分支分叉点 924a5a1，合并时机与范围待 Summer 排期。
- 全量 842 首跑曾报 rc=1：系输出管道截断 + 某用例模拟路径失败打印 `[exit] DEGRADED` 字样所致，完整日志复核为 `OK (skipped=3)`，非真实失败。

## Round 2 预审对账（2026-09-25）

**关键事实：B 本轮基于推送前旧快照审校**（其依据写明远端 1d=0388c21、f9b1495 不可见；A 已于此前推送 `230e0a3`）。B 自设可证伪条件——"若新测例 commit 已公开且分支头更新到该 SHA，则我错"——**已触发**。

| R# | 要点 | 终态 | 证据 |
|---|---|---|---|
| R2-1 | 测例未公开闭环 | ✅ 闭环（条件触发） | ls-remote 实时 1d=`230e0a3`；匿名 API commits/230e0a3 HTTP 200；公开版含对抗测例与 R1-5 措辞；f9b1495 经 fresh fetch 确认在 origin/1d（push 原子性） |
| R2-2 | "44"口径收紧 | ✅ A 认账（双错：口径+计数） | 全区间 70（B 对）；core/tests 子集精确值 **42**（A 报 44 系目测未复核）；新基准全区间 **72**（子集 43） |
| R2-3 | 双向分叉非单向落后 | ✅ 采纳 | main...230e0a3 = ahead 1 / behind 72；merge-base = 924a5a1 |
| R2-4 | 穿透从 artifact/report 血缘进入点开始 | ✅ 采纳 | 链序并入第 2 轮交接模板 |
| R2-5 / R2-6 | R1-4、状态机继续后置 | ✅ 一致 | 与 R1-4/R1-12、R1-6 处置相同 |
| R2-7 | 123 无前置执行 | ✅ 已执行 | `230e0a3` |

- 澄清 841/842：841 是 b49e125 提交信息里的时点数；842 是 A 在 `230e0a3` 内容上的实跑（`Ran 842 tests in 72.938s OK skipped=3`），非同一时点，无矛盾。
- f9b1495 匿名 API 单对象查询返回 None（配额/瞬时），不影响结论：fetch 后 origin/1d 包含该对象（服务端接受 230e0a3 必然已收下全部祖先）。

**第 2 轮正式审计基准不变：`230e0a3` · v4.4-a-final-1d · 审计区间 924a5a1..230e0a3（72 commits）。**

## Round 2 正式审计 · A 核实对账（2026-09-25）

**核实方法**：worktree 只读（`D:\PythonProject\_scratch\qtv-230e0a3` @ `230e0a3`）；远端 tip 复核 ls-remote：1d=`230e0a3`、main=`097ac100`。五段关键源码全读 + 离线攻击复现脚本 `D:\PythonProject\_scratch\r2_attack_bind_evidence.py`（临时 registry、零网络、含 2 个负例对照），8/8 断言 PASS。

| R# | B 指控 | A 终态 | 锚点（`230e0a3`） |
|---|---|---|---|
| R2-1 | bind_validation 不绑定 evidence↔report；伪造 schema-valid evidence 可入五门 | ✅ **实锤（P0，攻击复现）** | `core/model_registry.py` 583-586：evidence 仅过 schema；全程无 evidence↔report 内容比对。CLI `bind_validation_evidence.py` 61-77：`_check_evidence_against_registry` 只对 registry 三项。攻击输出：S1a CLI 端到端 → `[promotion] status=approved`；S1b evidence.provenance 三项全错（`"A"*64`）仍 bind=True → derive_promotion=**approved** → verify_approval=**True(ok)** |
| R2-2 | n_power_horizon 收录 schema 但五门不读 | ✅ 实锤 | 全 worktree grep 仅 `validation_schema.py:85`（POWER_KINDS 定义）1 处；门 1（model_registry.py 681-696）只读 n_power_fund。攻击输出：n_power_horizon 留 UNKNOWN 仍 approved |
| R2-3 | pooled 固定 n<30 与冻结 N_POWER_FUND 无统一量尺 | ✅ 实锤 | `backtest_forecast.py:280` `n < 30`；门 1 pooled 仅查 INSUFFICIENT_POWER 状态（686-687），pooled n 从不比 n_pf。攻击输出：pooled n=35 < n_pf=50 → **approved**；对照 fund n=35 < 50 → blocked_power |
| R2-4 | provenance「OK 文本」过松（placeholder-OK） | ✅ 实锤 | `_check_ev` text=非空字符串（validation_schema.py 189-192），`"unknown"` 合法；门 5（750-764）只查 status+两个枚举。攻击输出：frozen_dataset/feature_protocol/contract_version 等 6 槽全 `"unknown"` → approved |
| R2-5 | evidence/report 血缘统一为五件套 hash 对象 | 🤝 方向同意 | evidence.provenance.artifact_sha256 槽位存在但零消费（S1b 填错值照样过）；evidence 文件自身 SHA 未入档——修复方向成立，属设计项非缺陷 |
| R2-6 | 暂不重构 shadow prereg | 🤝 同意 | verify_approval（263-336）无一处调用 evaluate_prereg_degradation；shadow_policy.py 265/275/437 独立通道；本轮零 shadow 改动 |
| R2-7 | 裁决「evidence 不可伪造绑定 = 唯一阻断项」 | ⏳ 待 Summer | **A 建议是**；修 R2-1 时同分支顺带收 R2-2（明确定位）+ R2-4（placeholder-OK 钉死），三者同函数域一次改清，避免三轮往返 |
| R2-8 | n_power_horizon 是否真授权门 | ⏳ 待 Summer | Obsidian `量化交易工具/` 范围 grep N_POWER_HORIZON **0 命中**，契约 B §6 原文语义需 Summer 裁决；A 默认建议降级为记录字段（与 R2-2 同改） |

**负例对照**（证明门本身 fail-closed、只对 evidence 自报值负责）：fund n=35<50 → blocked_power；brier 0.70 劣于多数类 0.60 → blocked_performance。即缺口不在五门/A4，在**证据入口无不可伪造绑定**——与 B 的链条定性一致。

**处置**：审校冻结（§2.3），`230e0a3` 不动；待 Summer 裁决 R2-7/R2-8 后开修复分支落地，同时收编 R1-4/R1-12（bind 失败原因可见性，与 R2-1 修复同界面）。攻击脚本全文保留于 `_scratch/r2_attack_bind_evidence.py` 备 B 复核；临时 registry 用后即删，真实 registry 零改动。

## Round 3 开局 · GPT 两项裁决（2026-09-25，Summer 转贴，A 已核实并接受）

**R2-7 = (c)**：R2-1 是当前唯一「证据可信性阻断项」，**不是**唯一「生产授权条件」——契约本就要求多层合取，修完 R2-1 ≠ 生产授权前置全部满足。其余门（performance / baseline_edge / calibration）可并行细审，**R2-1 闭环前不得据此宣告 `model_ready`**。
**R2-8 = (c)**：契约 §6.2 要求冻结 N_POWER_FUND 与 N_POWER_HORIZON（属实），但可执行授权门（§6.1 F1）只写基金级 `n >= N_POWER`，推荐 schema（§13）只列 `n_power_fund` → **`n_power_horizon` 临时冻结口径：保持 UNKNOWN，不得用于 approved，也不得自创阻断**；等 Summer 补契约语义。既不接受 (a)（把"要求冻结"升级成"必须参与门"），也不接受 (b)（把契约已有字段擅自降格 research-only）。

**A 对 GPT 契约引用的独立核实**（`evidence/contracts/forecast_promotion_contract_B_20260923.md` @ `230e0a3`，9.4KB）：
- L90（§6.2）：「完成后写入并冻结 `N_POWER_FUND`、`N_POWER_HORIZON`」——引用属实
- L94（§6.1 F1）：「∀ fund: n >= N_POWER，否则 BLOCKED_POWER」——门仅基金级，属实
- L147（§13 推荐 schema）：power 只列 `n_power_fund`——属实
- L56（§4）：「Pooled Evidence AND Production-Fund Evidence AND Baseline Edge Evidence」——多层合取属实
- ⚠️ 检索盲区自曝：A 上轮只在 Obsidian 笔记范围 grep（0 命中），未查 QuantV1 仓库本体；本轮已补核。契约原文以仓库为准，笔记无副本。

**Round 3 组织**（A 提议，待 GPT 确认锁定）：并行审——A 开 R2-1 修复分支（不动 `230e0a3`）∥ B 并行细审 performance / baseline_edge / calibration / 状态机；`model_ready` 统一等待 R2-1 闭环。

**修复分支范围**（待 GPT 确认）：① R2-1 evidence↔report 不可伪造绑定（方向采 R2-5：report SHA + evidence 文件 SHA + model SHA + dataset SHA + git SHA 血缘对象入档；bind 层强制 + A4 复核扩到 evidence）；② R2-4 placeholder-OK 钉死；③ `n_power_horizon` 按 R2-8(c)：保持 UNKNOWN + 注释/测例钉死「未消费是预期，等契约补语义」；④ R1-4/R1-12 同界面顺带收。

**挂账契约补充项**（Summer，不阻塞本轮）：① horizon-level gate 语义（N_POWER_HORIZON 究竟管什么）；② pooled 样本量量尺（契约 §5 只有 RankIC/edge 条件、无 pooled n 阈值；现行为=验证器固定 n≥30，对应 R2-3 的最终归宿）。

## Round 3 锁定（2026-09-25，GPT 确认，A 已核实）

**组织**：并行审查 + 最终授权统一等待 R2-1。A 修 R2-1 主线；B 细审顺序 **baseline_edge → performance → calibration → 状态机**；所有线汇合到 R2-1 闭环后才进 `model_ready` 授权讨论。`N_POWER_HORIZON` 继续 UNKNOWN / 冻结 / 不消费 / 不作 APPROVED 条件。

**GPT 新增契约引用——A 核实 3/3 属实**（契约 @`230e0a3`）：
- §3.2（L41-51）：decision_edge = 同 OOS 样本同基金同 horizon 的 `RankIC(model) − RankIC(est_chg)`，禁换样本 → baseline_edge 优先理由成立
- §5.1/§5.2（L67-77）：正式条件 `decision_edge_CI_lower > 0`；绝对 RankIC 只证「模型自身能力」，不能替代 edge CI
- §9（L102-104）：区间校准 ≠ Alpha 证据 → calibration 放第三成立
- §12（L124-127）：「本批不动 derive_promotion——扩展属后续独立批次」→ 状态机最后审成立
- 附用锚点：§4（L55-61）六层合取（含 Frozen Provenance、Registry/Protocol Integrity，任一不满足 BLOCKED）；§8（L98-100）OOS CI 一律 cluster bootstrap by trading day，复用 `backtest_forecast.cluster_bootstrap_ci()` 不得另造第二套；§6.1（L85）`n < power_threshold → INSUFFICIENT_POWER`，不得解释为 PASS（025687 n=35）；§10（L106-108）0910 OOS 永久降级 development/selection，FINAL LOCKBOX 待四前置满足后一次生成

**两条治理约束（GPT 提出，A 核对现状后接受）**：
- **约束 1（evidence_sha 禁自哈希）**：口径定为 `evidence_sha256 := SHA256(evidence 文件原始字节)`，与 `report_sha256` 同法**带外存储**（registry 记录），永不写入 evidence 自身——结构上排除递归定义；规范化 envelope 方案留实现期细化。
- **约束 2（UNKNOWN 双层语义）**：现有代码已满足「授权不得把 UNKNOWN 当 PASS」（门 1-5 全部要求 `status==OK`，UNKNOWN → `blocked_*`）；R2-4 修复只收紧 **OK 态值的语义白名单**（sha 字段限 64-hex、`git_commit` 限 40-hex、文本字段 denylist `"unknown"`/`"n/a"` 等），不碰 UNKNOWN 合法性。
- 范围红线（GPT 明示，A 认）：不加范围，不碰 `n_power_horizon`，不自创 pooled power gate，不扩大 shadow。

**修复分支设计**（分支名 `v4.4-r2fix-evidence-lineage`，自 `230e0a3` 切出；设计稿已交 Summer 审批，未动工）：
- `core/validation_schema.py`：`_check_provenance` 加 OK 态白名单/denylist（约束 2 语义下实现 R2-4）
- `core/model_registry.py`：`bind_validation` 加 `evidence_file` 参 + 三向血缘核对（evidence.provenance 三项 vs report vs registry）+ `evidence_file`/`evidence_sha256` 入档（五件套：report_sha256 / evidence_sha256 / entry.sha256 / samples_sha256_lf / git_commit）；新增 `bind_validation_detailed` 返回结构化失败原因（R1-4/R1-12），原函数薄壳保持兼容；`verify_approval` A4 扩展：重读 evidence 文件、SHA 比对、schema 重验、evidence.provenance 三项 vs registry 重核
- `bind_validation_evidence.py`：必传 evidence 文件、传递 SHA、打印具体失败原因
- 测例 6 组：伪造 provenance 拒绑（S1b 攻击翻转）、占位符-OK 拒绝、UNKNOWN 仍合法、evidence 篡改/丢失 → verify_approval 拒、bind 失败原因含具体 mismatch 键
- 验收：全量测试绿 + `r2_attack_bind_evidence.py` 复跑 S1a/S1b 断言翻转为「被阻断」
- 稳定性承诺：门 3 与产出端（backtest_forecast）不动 → B 的 baseline_edge 行号锚点在修复后依然有效

## Round 3 落地（2026-09-25，A 修复分支已推送）

**新 sha：`3c86b240d8d4430d4289f9cbcb2ec54db8d7fc8c` · 分支 `v4.4-r2fix-evidence-lineage`**（自 `230e0a3` 切出，已推送远端；主工作区 `230e0a3` 冻结未动）。

**改动面**（commit `3c86b24`，7 文件 +643/−193；分支头另含一个 test-hygiene 尾随提交，以远端为准）：
- `core/model_registry.py`：`bind_validation` 加 `evidence_file` 参 + 三向血缘核对（`_check_evidence_lineage`：evidence 三身份 vs report PROVENANCE_JSON vs registry 逐项比对）+ `evidence_file`/`evidence_sha256` 入档（带外哈希 = SHA256(文件原始字节)，治理约束 1 禁自哈希）；新增 `bind_validation_detailed` 结构化失败原因（R1-4/R1-12），原函数薄壳保持 bool 兼容；`verify_approval` A4 扩展：重读 evidence 文件 → SHA 比对 → schema 重验 → 三向重核（置于既有各门后，历史拒绝原因不变）
- `core/validation_schema.py`：`_check_provenance` 身份字段 OK 态 hex 白名单（64-hex sha / 40-hex commit；UNKNOWN 合法性不动，治理约束 2）
- `bind_validation_evidence.py`：必传 evidence 文件 + 打印具体拒绝原因
- 门 3 / `backtest_forecast` / `n_power_horizon` / shadow：一行未动（三条红线全守）

**三层验收（全过）**：
1. 全量 `run_tests.py --all`：**853 tests OK（skipped=8，rc=0）**——较基准 842 +11（新增 `test_evidence_lineage.py`，已登记 FAST 层）；首轮 3 处失败中 2 处为漏登记（已修）、1 处 `test_reads_latest_frozen_file` 经**对照实验**证实为环境性（pristine `qtv-230e0a3` 新 worktree 同样复现「冻结样本缺失」；拷入运行时冻结样本文件后转绿；skipped 3→8 同源，环境条件性，非代码回归；主工作区为历史全绿基准环境）
2. 攻击复跑 `r2_attack_recheck_fix.py`：**8/8 PASS（期望翻转）**——S1a CLI 端到端伪造可入档但 promotion=`blocked_provenance`（reason 指向占位文本）、S1b 三身份全错被 schema hex 白名单直接拒绑（`evidence_schema_invalid:...R2-4 白名单`）、verify_approval 拒（`promotion_not_approved`）、S2/S2b/S2c 回归不变（R2-3 仍挂账契约，行为未动）
3. 新 sha 推送：ls-remote `v4.4-r2fix-evidence-lineage=3c86b24` ✓（推送收据在案）

**新增验收套件**：`tests/test_evidence_lineage.py`（11 测）——三向血缘拒绑 / 文件篡改与丢失拒授权 / 未文件绑定不得授权（`evidence_lineage_unbound`）/ schema 白名单与 UNKNOWN 合法 / 占位-OK 可存不可授权 / 结构化失败原因。

**待办移交**：① B 的 baseline_edge 细审在**新 sha** 上继续（行号锚点有效，远端分支已可读）；② R2-3（pooled 量尺）与 N_POWER_HORIZON 语义两项契约补充仍挂账 Summer；③ R2-1 修复已落地，正式闭环待 B 在新 sha 上复核；④ `model_ready` 授权讨论统一等 R2-1 闭环确认后开启。

## Round 3 审校对账（2026-09-25，B 第 3 轮 · 基准 `1927467`）

B 立场：identity-lineage 部分的 R2-1 可关闭；新 P0 = R3-1（身份真 ≠ 指标真，computation lineage 缺失）。B 自报全程静态审未实跑；A 逐条对码核实 **6/6 引用属实**，并**首跑实证**（攻击脚本 `r3_attack_metrics_forge.py`，7/7 PASS，全文保留备 B 复核）。

| R# | B 指控 | A 终态 | 锚点（`1927467`） |
|---|---|---|---|
| R3-1 | 真实血统 + 伪造指标可通过（computation lineage 缺失） | ✅ **实锤（A 实跑复现）** | `bind_validation_detailed`@639-708：schema → report 三项 → 三向血缘（仅三身份）→ 落档，无任何指标重算。S3 实证：伪造全过指标 + 真实身份 → bind 通过；重写 report（保留真实 PROVENANCE_JSON 行）→ 重新绑定成功、sha 换绑、仍 approved |
| R3-1 加严 | B 称「当前 frozen=False 使 APPROVED 结构性不可达」 | ⚠️ **B 说轻了** | `power.frozen` 是 evidence 自报值，无冻结预注册核对——S3b 实证伪造 frozen=True 当场 **approved**，旁路**今天就活**，不是"未来才有效" |
| R3-2 | CLI 复制一套血缘逻辑 | ✅ 实锤（接受，下批收） | CLI `_check_evidence_against_registry`@61 与 bind `_check_evidence_lineage`@608 双份并存，漂移风险成立 |
| R3-3 | registry git_commit = 登记时 HEAD | ✅ 实锤（语义待 R3-Q2 裁决） | `train_forecast_model.py` 全程无 git 透传（L78 仅 snapshot_provenance）→ `register_model`@148 → `capture_provenance`@89（登记时 `git rev-parse HEAD`）；且代码 docstring 多处自称"训练代码 commit"，语义悬空 |
| R3-4 | 报告侧 base_ic 走旧外壳哨兵 0.0 | ✅ 实锤（诊断层，接受，下批收） | `rank_ic_status`@117（NOT_COMPUTABLE 核心）vs `rank_ic`@131 外壳（docstring 自认"历史哨兵 0.0 **不是测量值**"）；报告路径 L771 用外壳，证据层走 status——双轨真实存在 |
| R3-5 | contract_version/feature_protocol 仅字符串合法 | ✅ 实锤（接受） | `CONTRACT_VERSION="forecast-v1"`@forecast_contract.py:59 有现成机械相等锚点但未比对；feature_protocol 未与 registry 协议对账（verify_approval 协议门可挡一部分） |
| R3-6 | baseline_edge 公式级无错误 | ✅ 与 A 契约锚点核实一致 | §3.2/§5.1/§8 对照（同 OOS 样本差分 + 联合 cluster bootstrap + pooled/基金 CI 下界>0）——B 的并行细审线首项通过 |
| R3-7 | identity/computation lineage 两级词汇 | 🤝 采纳 | 台账与后续报告启用两级术语 |
| R3-8 | 缺「真身份+假指标」测例 | 🤝 采纳 | S3 即该测例（当前红）；修复落地后转正为永久回归测例 |
| R3-9 | evidence_file 统一相对路径 | 🤝 P2 挂账 | 不阻塞 |

**交 Summer 裁决（R3-Q1/Q2 + 修法路线）**：
- **R3-Q1（威胁模型定性）**：是否含「防可信操作员伪造 metrics」？**A 建议：是，R3-1 维持 P0**——registry + 五门 + A4 整套架构的存在意义就是让 evidence 可计算回溯；若采信"操作员可信"则全链降级为手动声明，唯 manual gate 只应保留 `config.model_ready` 一处（B++-3 起既定原则）。B 的可证伪条件（威胁模型明确采信操作员）需要 Summer 明示，不能由 A 默认。
- **R3-Q2（git_commit 语义）**：**A 建议：定义为「训练代码 commit」**，修训练侧透传（训练开始前捕获 HEAD → 随产物传递 → register 覆写），登记时 HEAD 仅作 fallback 并显式标注。
- **修法路线（R3-1）**：A. producer receipt（验证器产出回执，绑定阶段核验 receipt 与 report/evidence/artifact/dataset 绑定）为下批落地；B. 绑定时重算门 1-4 为终态目标（需 bind 可执行回测，成本高）。**A 倾向 A 路线先行、B 路线列入 V5 方向**，由 Summer 拍板。
- 附：R3-8 的「真身份+假指标应拒绝」测例在修复落地前以 S3 形式作红灯展示，落地后转正回归。

**待办**：修复设计稿待 Summer 批（R3-Q1 定性 + 路线选择）→ 下批分支；R2-1 闭环结论暂改写为「identity-lineage 闭环，computation-lineage 待修」。
